# Week 1 Senior Review — Full Findings Appendix (2026-06-10)

Machine-generated appendix to `2026-06-10-week-1-senior-trainer-review.md`.
65 confirmed findings from a 56-agent workflow (7 finder dimensions, adversarial verification of every critical/major finding, completeness critic).
All findings verified as NOT duplicating the 2026-05-31 content audit.


## CRITICAL (8)

### F1. Lab 1.6 Grafana SSH tunnel command uses wrong bastion user and author-leftover key filenames — remote learners cannot reach Grafana (Core-path blocker)
- **Dimension:** access | **Verification:** high
- **File:** `curriculum/week-1-foundations/labs/lab-1.6-deploy-kof.md` L748-757
- **Evidence:** Lab 1.6 L749-754: "ssh -o StrictHostKeyChecking=no ... -o ProxyCommand=\"ssh -i ./config/keys/bastion.pem -o StrictHostKeyChecking=no -W %h:%p ubuntu@<BASTION_IP>\" \\\n    -i ./config/keys/lab-validation-k0rdent.pem \\ ...". But lab-infrastructure/scripts/lab-provision.sh L83-85 states "# SSH options for bastion jump (bastion is Amazon Linux = ec2-user)" and uses "ec2-user@${bastion_ip}"; provisioning saves keys as "${engineer_id}-k0rdent.pem" and "${engineer_id}-bastion.pem" (lab-provision.sh L325-328). Files named bastion.pem and lab-validation-k0rdent.pem never exist for students ('lab-validation' is the author's own engineer-id), and ubuntu@bastion is the wrong user — the command fails with 'No such file' / 'Permission denied'.
- **Fix:** Replace the hand-rolled tunnel with the existing supported path: ./scripts/lab-connect.sh <your-engineer-id> --tunnel 3000:3000. If the raw ssh form is kept for teaching, fix it to ec2-user@<BASTION_IP>, -i ./config/keys/<your-id>-bastion.pem (proxy) and -i ./config/keys/<your-id>-k0rdent.pem (target), with placeholders explained.
- **Verifier note:** Verified verbatim: lab-1.6-deploy-kof.md L749-754 uses ubuntu@<BASTION_IP> for the bastion jump and key files ./config/keys/bastion.pem and ./config/keys/lab-validation-k0rdent.pem. lab-provision.sh L83-85 confirms the bastion is Amazon Linux (ec2-user), and L325-328 saves keys as ${engineer_id}-k0rdent.pem / ${engineer_id}-bastion.pem — so both filenames never exist for students and the bastion user is wrong; the command fails on identity-file lookup and/or Permission denied. 'lab-validation' occurs nowhere else in the repo, consistent with an author-leftover engineer-id. Part 4 (Grafana access) is explicitly on the Core path (L100 table: Core = Parts 1-4, 7), so the broken tunnel blocks the recommended path's only documented browser route to Grafana. The recommendation is sound: lab-connect.sh already implements --tunnel <local:remote> (usage L33, handler L142-145), matching the access pattern every other Week 1 lab uses. Not covered by the prior audit (its Lab 1.6 items are the difficulty cliff, Core table skipping Part 5, and Grafana binding 0.0.0.0 — none address the tunnel user/key bug). Only caveat: 'cannot reach Grafana' is slightly absolute since a learner could independently discover lab-connect.sh --tunnel, but the documented command is broken as shipped, so critical severity is defensible.

### F2. Lab 1.5 cleanup section tells learner to delete the managed cluster that Labs 1.6/1.7 require — the 'do not clean up' warning only appears in the NEXT labs
- **Dimension:** crossweek | **Verification:** high
- **File:** `curriculum/week-1-foundations/labs/lab-1.5-provision-managed-cluster.md` L568-580 (vs lab-1.6 L74, lab-1.7 L73)
- **Evidence:** lab-1.5 L568-576: "## Part 7: Clean Up (~10 min)\n> **Important:** Clean up resources to avoid unnecessary cloud costs!\n### Delete Managed Cluster(s)\nkubectl delete clusterdeployment managed-cluster-01 -n kcm-system" — with NO caveat about Labs 1.6/1.7 anywhere in lab 1.5 (grep for 'keep|Lab 1.6|Lab 1.7' finds only L638 'Continue to [Lab 1.6...]', which comes AFTER the cleanup part). The counter-instruction exists only in the following labs: lab-1.6-deploy-kof.md L74: "**Keep your managed cluster running!** This lab requires the managed cluster from Lab 1.5 to be active. Do **not** run the Lab 1.5 cleanup steps until you have completed Labs 1.6 and 1.7." and lab-1.7 L73 (same warning).
- **Fix:** Move the keep-running warning into Lab 1.5 itself: gate Part 7 with 'Skip this section if you are continuing to Labs 1.6-1.8 (recommended); the managed cluster is required there.' A linear reader executes Part 7 before ever opening Lab 1.6, then must re-provision a cluster (~20-30 min + cost) to continue.
- **Verifier note:** Verified verbatim: lab-1.5 Part 7 (L568-580) unconditionally deletes the managed cluster with no keep-running caveat anywhere in the file (only Lab 1.6 mention is the Next Lab link at L638, after cleanup), while lab-1.6 L74 and lab-1.7 L73 both say 'Do not run the Lab 1.5 cleanup steps until you have completed Labs 1.6 and 1.7'. Stronger than claimed: lab 1.5's Validation Checklist (L616) requires '[ ] Cleaned up all resources' as a completion criterion, actively pushing linear learners to delete. Not a duplicate: prior-audit C1 covers the opposite direction (lab-destroy.sh never deleting ClusterDeployments at final teardown), and the audit's lab-1.5 row flags only the Azure path (C5); the premature-deletion sequencing contradiction is unreported. Technically sound for k0rdent/CAPA: deleting the ClusterDeployment tears down the cluster's VPC/EC2, forcing a ~20-30 min re-provision + cost. Recommendation (gate Part 7 with a skip-if-continuing note) is a cheap, real fix. One caveat: severity is better rated major than critical — blast radius is one bounded re-provision, not unbounded spend or data loss.

### F3. End-of-week environment contradiction: Lab 1.8 says NEVER destroy the management cluster ('reused in all subsequent weeks') but Weeks 2-3 never use it, and Week 1 README's cost guidance assumes teardown
- **Dimension:** crossweek | **Verification:** high
- **File:** `curriculum/week-1-foundations/labs/lab-1.8-upgrade-k0rdent.md` L634-636 (vs README.md L115-121, lab-2.1 L12, lab-2.2 L11-19)
- **Evidence:** lab-1.8 L634: "> **Do NOT run `lab-destroy.sh` after completing Week 1.** Your management cluster is reused in all subsequent weeks. Week 5 GPU labs create GPU clusters via `ClusterDeployment` on this same management cluster... If you need to pause between weeks, simply disconnect." vs week-1 README L115-121 cost table listing every resource as active only "Labs 1.1-1.7" with "Running 8 hours/day for 5 days: ~$15-25 total. Remember to destroy environments when not in use". Meanwhile Week 2's labs don't touch this cluster at all: lab-2.1 L12 "Access to k0rdent sandbox environment (credentials provided separately)", lab-2.2 L12-13 "Lab environment provisioned (Metal3 Dev type) / SSH access to lab VM"; Week 3 lab-3.3 L5 uses "GPU Lab (P4d.24xlarge)". The next lab that actually reuses the management cluster is Week 5 (lab-5.1 L66).
- **Fix:** Reconcile the end-state contract: either (a) instruct teardown after Week 1 and re-provision before Week 5 (lab-provision.sh already rebuilds in ~30 min), or (b) keep it but add explicit idle-cost guidance (~$0.24/hr = ~$40/idle week for t3.xlarge + NAT + NLB across the 2-3 weeks that never use it). Currently a compliant learner pays for 3+ weeks of idle infrastructure on the strength of L634.
- **Verifier note:** All cited evidence verified verbatim. lab-1.8 L634-636 forbids teardown claiming the management cluster is "reused in all subsequent weeks," but Weeks 2-3 verifiably use separate environments (lab-2.1 sandbox w/ separate creds, lab-2.2 Metal3 Dev VMs, lab-3.3 GPU P4d) with zero references to the Week 1 cluster or lab-connect.sh; first real reuse is Week 5 lab-5.1 (~L66). Week 1 README L115-121 cost table marks all resources active only "Labs 1.1-1.7" with "$15-25 total" and "destroy environments when not in use" — direct contradiction. No pause/stop script exists (lab-infrastructure/scripts/ has only connect/destroy/provision/refresh-creds/status), so "simply disconnect" leaves t3.xlarge + NAT + NLB (~$0.24/hr, ~$40/idle week) billing 24/7; rates match README's own figures. Not a duplicate: prior audit C1 covers the destroy-path orphan problem, and the audit at L118 actually praises the Lab 1.8 preservation note as a strength, so this contradiction is affirmatively uncaptured (only mild adjacency to C1's fix mentioning Lab 1.8 teardown). Recommendation is actionable and materially improves a paid course. Sole caveat: severity reads a notch high — bounded ~$40-80 exposure suggests major/high rather than critical (C1's unbounded orphan cost is the true critical).

### F4. Weeks 2 and 6 (including the graded capstone) are built on a fabricated k0rdent product surface — 'Provider Console', 'Customer Console', and a REST API (/api/v1/tenants, /api/v1/auth/token) that Week 1 never taught and k0rdent Enterprise 1.2.2 does not have
- **Dimension:** crossweek | **Verification:** high
- **File:** `curriculum/week-2-bmaas/labs/lab-2.1-explore-sandbox.md` L25-77 (and week-6 lab-6.6 L53-81)
- **Evidence:** lab-2.1 L27 "Log into Provider Console", L57-61: "curl -X POST https://<k0rdent-url>/api/v1/auth/token ... -d '{\"username\": \"<user>\", \"password\": \"<pass>\"}'", L67 "curl -X GET https://<k0rdent-url>/api/v1/tenants", L84 "Switch to Customer Console". The capstone repeats it: lab-6.6 L53 "**Via Provider Console:** Navigate to Tenants → Create New", L61 "curl -X POST https://<k0rdent>/api/v1/tenants", L88 "curl -X POST https://<k0rdent>/api/v1/tenants/$TENANT_ID/networks". Week 1 teaches the opposite model throughout: a single k0rdent UI plus CRDs via kubectl (week-1 README L94 "Envoy Gateway → k0rdent UI (:3000)"; ClusterDeployment/Credential/MultiClusterService in labs 1.3-1.7). No tenant REST API, token endpoint, or dual console is ever introduced, and none exists in k0rdent Enterprise 1.2.2. Per overview L126 the capstone is 20% of the grade.
- **Fix:** Rewrite Lab 2.1 and capstone Phase 1 against the real surface taught in Week 1 (Namespaces + AccessManagement/RBAC CRDs, ClusterDeployment, MultiClusterService, k0rdent UI), or clearly mark these as a separate non-k0rdent demo product. As written, the trainee's first post-Week-1 task is to authenticate to an API that does not exist.
- **Verifier note:** Verified verbatim: lab-2.1 (Provider Console L27, POST /api/v1/auth/token L59-61, GET /api/v1/tenants L67, Customer Console L86) and capstone lab-6.6 (Provider Console Tenants->Create New L53-54, POST /api/v1/tenants L61, /tenants/$TENANT_ID/networks L88); overview table confirms capstone = 20% of grade. k0rdent OSS/Enterprise is CRD-only via the Kubernetes API per docs.k0rdent.io and docs.mirantis.com — no tenant REST API, token endpoint, Tenant resource, or Provider/Customer console split exists; Week 1 of this repo teaches the real surface (kubectl + CRDs + single UI :3000), and its only /api/v1 strings are VictoriaMetrics/K8s paths. Fabrication is broader than cited: lab-2.3 L150 also curls nonexistent /api/v1/hosts. Not covered by the Week-1-scoped prior audit (closest, C3, is a different fabricated artifact). Minor citation nit: 'Switch to Customer Console' is L86, not L84. Recommendation is sound — capstone Phase 1 is unexecutable as written.

### F5. Lab 1.5 instructs students to delete the managed cluster that Labs 1.6/1.7 require — the only warning lives downstream in Lab 1.6
- **Dimension:** drilldown | **Verification:** high
- **File:** `curriculum/week-1-foundations/labs/lab-1.5-provision-managed-cluster.md` L568-616 (and lab-1.6 L74)
- **Evidence:** Lab 1.5 Part 7 (L568-580): "## Part 7: Clean Up (~10 min)\n> **Important:** Clean up resources to avoid unnecessary cloud costs!\n# Delete the AWS cluster\nkubectl delete clusterdeployment managed-cluster-01 -n kcm-system" and Validation Checklist L616: "- [ ] Cleaned up all resources". The contradicting warning appears only in lab-1.6 L74: "**Keep your managed cluster running!** This lab requires the managed cluster from Lab 1.5 to be active. Do **not** run the Lab 1.5 cleanup steps until you have completed Labs 1.6 and 1.7." — which a sequential learner reads only AFTER deleting the cluster. Lab 1.6 Part 3 then labels the now-nonexistent cluster: "kubectl label clusterdeployment managed-cluster-01 ... -n kcm-system" (L381-383).
- **Fix:** Move the keep-it-running warning into Lab 1.5 Part 7 itself (e.g., 'SKIP this section if continuing to Labs 1.6-1.8 today; cleanup is repeated at end of Lab 1.7/1.8'), remove 'Cleaned up all resources' from the Lab 1.5 checklist, and add a cost note ('~$0.15/hr while it stays up') instead.
- **Verifier note:** All quoted evidence verified. lab-1.5 Part 7 (L568-580) unconditionally deletes managed-cluster-01 and the L616 checklist requires "Cleaned up all resources", with no keep-running caveat anywhere in the file (the Next Lab pointer is at L638, after cleanup). lab-1.6-deploy-kof.md L74 contains the downstream warning verbatim, lists "Completed Labs 1.1-1.5" as a prerequisite, has no re-provisioning fallback, and its label commands at L381-391 would fail NotFound. Lab 1.7 L73/Part 9 confirm the cluster must survive through end of week and that deferring cleanup fits existing structure. Technically correct: ClusterDeployment deletion triggers full CAPI teardown. Not covered by prior audit (C1/C5/Lab-1.6-no-cleanup are adjacent but distinct). Minor quibbles: severity arguably major rather than critical (recoverable via ~15-20 min re-provision, and the failure mode saves cost rather than incurring it), and the lab-1.6 label commands span L381-391 not L381-383.

### F6. Week 1 footprint sits exactly at AWS default EIP/VPC service quotas — any cohort in a shared account (or any retry/second cluster) fails mid-Lab-1.5/1.6 with AddressLimitExceeded/VpcLimitExceeded, and no quota guidance exists anywhere in the curriculum
- **Dimension:** floor | **Verification:** high
- **File:** `lab-infrastructure/terraform/modules/networking/main.tf` L152-171 (plus bastion/main.tf 97-106, bastion/variables.tf 30-34, lab-1.5 L180-211, lab-1.6 L611)
- **Evidence:** Per-student management stack allocates 2 EIPs: networking/main.tf L152 'resource "aws_eip" "nat"' + L162 'resource "aws_nat_gateway" "main"', and bastion/main.tf L97 'resource "aws_eip" "bastion"' with variables.tf L30-33 'variable "use_elastic_ip" ... default = true'. Lab 1.5 then provisions managed-cluster-01 into a CAPA-managed VPC — lab-1.6-deploy-kof.md L611: 'When CAPA (Cluster API for AWS) provisions managed clusters, it creates a **new VPC** for each cluster' — whose default CAPA topology adds one NAT gateway (one EIP) per AZ, up to 3. That is ~5 EIPs for ONE student, exactly the AWS default quota of 5 EIPs/region; Labs 1.6-1.8 require keeping mgmt + managed cluster running concurrently. VPCs: 2 per student + the account default VPC against a default quota of 5/region — a 2-3 student cohort in one account/region is mathematically guaranteed to fail, and CAPA failure is the worst kind: VPC/subnets created, NAT allocation fails, ClusterDeployment reconciles forever while the half-built VPC bills. Verified by grep: 'quota|EIP|elastic ip|service limit' appears nowhere in curriculum/week-1-foundations/ or lab-infrastructure/README.md/docs — the only 'quota' hits are Kubernetes ResourceQuota content in Lab 1.4. (Corroborated by .auto-claude/specs/012-.../qa_report.md report RSRC-2 'AWS quota exceeded (VPCs, IGWs, NAT Gateways) | Medium | Undocumented', which underrated severity by not doing the per-student arithmetic.)
- **Fix:** Add a pre-flight quota section to Lab 1.1/README: required quotas per student (≥10 EIPs, ≥5 VPCs, NAT gateways, vCPU on-demand limit), an 'aws service-quotas' check snippet, and an explicit instructor rule: one student per account+region OR pre-raised quotas. Add an EIP/VPC headroom check to lab-provision.sh check_prerequisites. Document that a stuck 'AddressLimitExceeded' ClusterDeployment must be deleted to release the half-built VPC.
- **Verifier note:** Verified end-to-end. Per-student management stack allocates exactly 2 EIPs (networking/main.tf L152-171 NAT EIP; bastion/main.tf L97-106 with use_elastic_ip default=true in variables.tf L30-34, not overridden by environments/student-lab/main.tf). Lab 1.5 L180-211 deploys aws-standalone-cp with no VPC config; the k0rdent template's AWSCluster sets only additionalControlPlaneIngressRules, so CAPA defaults apply: availabilityZoneUsageLimit=3, one NAT gateway/EIP per AZ (CAPA issue #1752 documents this exact AddressLimitExceeded mode). Total: 5 EIPs for one student = exactly the AWS default 5/region; 2 students in the designed shared-account model (engineer_id + cohort_id variables, per-student state in one account) = guaranteed failure. VPC math (2/student + default VPC vs quota 5) also holds. No EIP/VPC quota guidance anywhere: week-1 'quota' hits are all Kubernetes ResourceQuota; lab-provision.sh check_prerequisites (L203-239) checks only terraform/aws-cli/credentials. One nuance the finding missed: lab-infrastructure/docs/troubleshooting.md L173-181 does cover VcpuLimitExceeded with a Service Quotas pointer — but not EIP/VPC/NAT, and not in the curriculum. Minor overreach: 'any retry fails' is too strong (clean deletion releases EIPs; only stuck/leaked clusters block retries), and in regions with <3 AZs the CAPA EIP count would be lower — but all standard training regions have 3+. Not covered by the prior audit (C1 is cleanup orphans, not quotas). The recommendation (pre-flight quota doc + provision-script headroom check + stuck-ClusterDeployment guidance) is proportionate and genuinely valuable for a paid cohort-based course. Severity critical is defensible for the shared-account cohort scenario; major if instructors mandate one account per student (nothing currently documents that rule).

### F7. lab-provision.sh captures the entire 15-20 min provisioning stream into the ui_url variable: no live progress is shown, the printed UI URL is a multi-hundred-line blob, and on any init timeout set -e kills the script before the password banner
- **Dimension:** infra | **Verification:** high
- **File:** `lab-infrastructure/scripts/lab-provision.sh` L352-365 (function 76-165)
- **Evidence:** Line 354: `ui_url=$(wait_for_gateway_url "$bastion_ip" "$mgmt_ip" "$mgmt_key" "$bastion_key")`. Everything inside wait_for_gateway_url writes to stdout: `log_info() { echo -e "${BLUE}[INFO]${NC} $1"; }`, line 107 `log_info "Streaming initialization progress (live)..."`, the ssh tail -f stream at lines 109-133 (`echo "  \$line"`), the progress dots, and line 145 `log_success "k0rdent initialization complete"` — all captured by the command substitution, never displayed. The success URL `echo "http://${gw_addr}"` (line 155) is buried inside that captured blob, which is then printed verbatim at line 362 `echo "  URL:       ${ui_url:-not yet available...}"`. On the failure paths (line 103 / line 143 `return 1`), with `set -euo pipefail` (line 5) the assignment's non-zero status aborts the whole script, so the `terraform output -raw ui_password` banner (lines 357-365) never prints, and even the captured `log_warn "Initialization did not complete..."` (line 142) is lost inside the dead substitution — the learner gets a silent terminal for ~20 minutes followed by either garbage output or an unexplained exit during Lab 1.1.
- **Fix:** Redirect all log_* output and the ssh progress stream inside wait_for_gateway_url to stderr (or tee to /dev/tty), echo ONLY the URL on stdout, and guard the call site with `ui_url=$(wait_for_gateway_url ...) || true` so a timeout still prints the credentials banner plus a clear retry hint.
- **Verifier note:** Verified against lab-infrastructure/scripts/lab-provision.sh. All cited evidence is accurate: line 354's bare assignment ui_url=$(wait_for_gateway_url ...) captures every byte the function emits (log_info/log_success at lines 88/107/145/148, SSH-wait and poll dots at 96/159, the filtered tail -f init stream at 109-133 with ssh stderr nulled, and the URL echo at 155) — nothing writes to stderr, so the learner sees a silent terminal for the ~15-20 min cloud-init wait and the 'live streaming' is a no-op. The banner at line 362 then prints the entire captured multi-line blob (the 'multi-hundred-line' size is plausible but unverifiable; tens of lines minimum). The set -e abort claim is correct for the two return-1 paths (line 103 SSH-unreachable, line 143 init-incomplete): because 'local ui_url' (353) is separate from the assignment (354), the substitution's exit 1 propagates and kills the script before the ui_password banner (357-365), losing both credentials and the captured log_warn diagnostics. Minor imprecision: the third degraded path (gateway LB address timeout, lines 163-164) falls off the function end returning 0 via 'echo ""', so it does NOT abort — it just yields a URL-less blob. Not covered by any prior-audit item (those touch lab-destroy.sh, production-readiness.sh, k0sctl.yaml.tpl, ttl_hours — not this script). Recommendation (stderr for progress, stdout for URL only, '|| true' guard) is the standard fix and materially improves the Lab 1.1 first-run experience. Severity 'critical' is defensible given this is the course's entry-point script; 'major' would also be reasonable since lab-connect.sh --ui-url and terraform output provide recovery paths.

### F8. Lab 1.5 mandates deleting the managed cluster that Labs 1.6 and 1.7 require — sequential learners destroy their own prerequisite
- **Dimension:** pedagogy | **Verification:** high
- **File:** `curriculum/week-1-foundations/labs/lab-1.5-provision-managed-cluster.md` L568-580, 616 (vs lab-1.6-deploy-kof.md:74)
- **Evidence:** Lab 1.5 Part 7 is an unconditional step: '## Part 7: Clean Up (~10 min)' → '# Delete the AWS cluster / kubectl delete clusterdeployment managed-cluster-01 -n kcm-system', and the lab's Validation Checklist gates completion on '- [ ] Cleaned up all resources'. Lab 1.6 then opens with: '> **Keep your managed cluster running!** This lab requires the managed cluster from Lab 1.5 to be active. Do **not** run the Lab 1.5 cleanup steps until you have completed Labs 1.6 and 1.7.' The warning lives only in the NEXT lab, after the destructive act.
- **Fix:** Move the keep-it-running warning into Lab 1.5 itself: convert Part 7 to 'Clean Up (skip if continuing to Labs 1.6-1.7)' with a prominent callout at the top of the part, and change the checklist item to 'Cleaned up resources OR confirmed cluster retained for Labs 1.6-1.7'. A sequential learner currently loses 20+ minutes and incurs re-provisioning cost on the already-overloaded Day 5.
- **Verifier note:** Verified verbatim. Lab 1.5 (lines 568-580) unconditionally deletes managed-cluster-01, the checklist (line 616) gates completion on "Cleaned up all resources", and the intro (lines 71-72) reinforces "Remember to clean up". The keep-it-running warnings exist only in Lab 1.6 (line 74) and Lab 1.7 (line 73) — after the destructive step. No mitigating warning exists anywhere else in week-1 content (grep confirmed). Not covered by the prior audit (C1 is the inverse: lab-destroy.sh never deleting ClusterDeployments). Recommendation is sound; Lab 1.7's own conditional-cleanup phrasing is the model to copy into Lab 1.5. Severity arguably major rather than critical (recoverable via ~15-25 min re-provision plus cloud cost), but the finding stands.


## MAJOR (38)

### F9. Cheat sheet instructs 'Destroy environment (end of day!)' — direct contradiction of Lab 1.1's 'only destroy after ALL labs'; literal readers (especially ESL) will destroy the week-long management cluster nightly
- **Dimension:** access | **Verification:** high
- **File:** `curriculum/week-1-foundations/resources/cheat-sheets.md` L239-240
- **Evidence:** cheat-sheets.md L239-240: "# Destroy environment (end of day!)\n./scripts/lab-destroy.sh <engineer-id>". Lab 1.1 (labs/lab-1.1-provision-k0rdent.md L523) says the opposite: "**Important:** Only destroy if you're done with **all labs in the curriculum**. The management cluster is reused in every subsequent week... If you need to stop for the day, leave the management cluster running and use lab-connect.sh to reconnect later." The cheat sheet is explicitly framed (L3) as "Keep this open alongside your labs."
- **Fix:** Change the cheat-sheet comment to '# Destroy environment (ONLY after finishing all labs — see Lab 1.1 Clean Up; to pause for the day, just disconnect)'. Idiomatic exclamation imperatives are taken literally by non-native readers; align all destroy guidance with Lab 1.1.
- **Verifier note:** Verified verbatim: cheat-sheets.md L239-240 says '# Destroy environment (end of day!)' while lab-1.1-provision-k0rdent.md L523 explicitly says only destroy after ALL labs and to leave the cluster running overnight (management cluster reused through Week 5 GPU labs). Cheat sheet L3 confirms it is meant to be kept open alongside labs. Direct contradiction; literal compliance destroys week-long state nightly. Distinct from prior-audit C1 (lab-destroy.sh resource-orphaning behavior) and the ttl_hours item — neither covers the when-to-destroy guidance conflict. Recommendation is a proportionate one-line fix.

### F10. Lab 1.2 (UI-exploration lab) Part 3 dashboard exercise has no non-visual path: zero screenshots or UI-layout description in the whole lab, and the dashboard checklist can only be completed by visually scanning the UI
- **Dimension:** access | **Verification:** high
- **File:** `curriculum/week-1-foundations/labs/lab-1.2-explore-k0rdent-ui.md` L384-411
- **Evidence:** Part 3 (L384 "## Part 3: Dashboard Overview (~5 min)") exercise L405-410: "Take 10 minutes to explore the dashboard:\n- [ ] Identify the cluster count\n- [ ] Note any alerts or warnings\n- [ ] Find the resource utilization section". Unlike Parts 4-8 (which pair every UI step with kubectl equivalents, e.g. L433-443), Part 3 offers no CLI alternative, and grep for '![' across all Week 1 markdown returns zero images — the 2.5h UI lab has no screenshots or textual description of the UI layout to orient any learner, sighted or not. Note also the internal time contradiction: header says '~5 min', exercise says 'Take 10 minutes'.
- **Fix:** Add CLI equivalents for each Part 3 checklist item (e.g., cluster count: kubectl get clusterdeployments -A --no-headers | wc -l; alerts: kubectl get events -A --field-selector type=Warning) and a short prose description of the dashboard regions (top nav, left sidebar entries in order, main summary cards). Reconcile the 5 vs 10 minute timing.
- **Verifier note:** Verified: L384 header says '~5 min' while L407 exercise says 'Take 10 minutes' (contradiction confirmed); L408-410 checklist has no kubectl equivalents while Parts 2, 4, 5, 6, 7, 8 all pair UI exploration with kubectl commands; grep for '![' across all of curriculum/week-1-foundations/ returns zero images, so the 2.5h UI lab (Duration confirmed at L3) has no screenshots. One caveat: the claim of zero 'textual description of the UI layout' is slightly overstated — L388-403 'Key Dashboard Elements' lists dashboard content categories (Cluster Summary, Recent Activity, Quick Actions) and L418 mentions 'left navigation' — but these list content, not layout regions, and give no way to verify checklist answers. Recommended CLI equivalents are technically valid for k0rdent (clusterdeployments CRD count, Warning events), noting cluster count will be 0 pre-Lab-1.5. Not covered by any prior-audit item (the only prior Lab 1.2 item is the fabricated OIDC schema). Recommendation is concrete and would materially improve the lab; severity 'major' is defensible, 'minor-to-major' at worst.

### F11. Architecture content conveyed only as ASCII box-drawing diagrams with no text alternative — screen readers render box characters as noise; entire sections carry zero accessible content
- **Dimension:** access | **Verification:** high
- **File:** `curriculum/week-1-foundations/theory/1.1-k0rdent-architecture.md` L331-354 (also theory/1.2 L56-73 and L355-374; README.md L76-109)
- **Evidence:** Theory 1.1 "### Deployment Topology" (L331-354) consists solely of a ┌─┐│ diagram (tenant clusters k0s/EKS/Metal3 under management cluster) with no accompanying prose — the section ends at the divider. Theory 1.2 "### Architecture" for KCM (L56-73) and KSM (L355-374) name internal components (kcm-controller-manager, Provider Controllers, ProjectSveltos addon-controller, Catalog/Source objects) only inside box art. Week-1 README "## Lab Architecture" (L78-109) maps labs 1.1-1.7 onto infrastructure only via a +--+ diagram, with no prose equivalent before the next section (L111). Counts: 61/110/81 box-drawing lines in theory 1.1/1.2/1.3 respectively. By contrast, the KOF section (theory 1.2 L559-564) shows the right pattern: diagram followed by a bulleted prose equivalent.
- **Fix:** Add a one-paragraph or bulleted prose equivalent immediately after each diagram-only section (follow the existing KOF pattern at theory/1.2 L559-564). Priority order: README Lab Architecture, Theory 1.1 Deployment Topology, Theory 1.2 KCM/KSM Architecture, Theory 1.3 Metal3 state machine (L313-343, whose 'Inspecting → Available' back-arrow ◄ is conveyed only spatially).
- **Verifier note:** Verified: theory/1.1 L331-354 (Deployment Topology) and README.md L76-109 (Lab Architecture) are genuinely diagram-only sections with no prose equivalent before the next heading; theory/1.3 L313-343 Metal3 state machine's Inspecting→Available transition is conveyed only by the ◄ glyph; the KOF counter-pattern (1.2 L533-557 diagram + L559+ bullets) exists as cited. Box-line counts 61 (1.1) and 81 (1.3) match exactly; 1.2 is ~118 vs claimed 110 (trivial char-class difference). One overstatement: theory/1.2's KSM/KCM internals are NOT exclusively in box art — addon-controller, CAPA, cert-manager, Flux sources are described in accessible tables later in the same file (L507-521, L607-669); only the Architecture subsections themselves are diagram-only, so 1.2 alone would be minor. Not covered by any prior-audit item (no accessibility findings in that list). Recommendation is cheap, concrete, and follows an existing in-repo pattern; relevant for WCAG/508-conscious enterprise buyers. Net: real, major for 1.1/README instances, slightly overstated for 1.2.

### F12. Terraform-version remediation is macOS/Homebrew-only in both Lab 1.1 and the provisioning script; supported OS (bash/WSL requirement) never stated — non-macOS trainees are stranded at the first prerequisite gate
- **Dimension:** access | **Verification:** high
- **File:** `curriculum/week-1-foundations/labs/lab-1.1-provision-k0rdent.md` L447-463
- **Evidence:** Lab 1.1 L447-459: "**Error: Terraform version too old**\n\n```bash\n# Install newer Terraform (macOS)\n... brew uninstall terraform ... brew tap hashicorp/tap\nbrew install hashicorp/tap/terraform" — no Linux or Windows instructions anywhere. lab-infrastructure/scripts/lab-provision.sh L207-209 likewise: 'log_error "terraform is not installed. Please install Terraform >= 1.8.0"; log_error "  brew install hashicorp/tap/terraform   # macOS"' and L221-224 repeat brew-only steps on version failure. Lab 1.1 Prerequisites (L45-50) list only 'Basic terminal/shell knowledge' — never that the tooling is bash-only (Windows users need WSL).
- **Fix:** Add Linux instructions (apt repo or direct zip from releases.hashicorp.com, or tfenv) next to every brew suggestion, and state in Lab 1.1 Prerequisites that scripts require bash on Linux/macOS/WSL. Update the script's error hints to offer at least one non-Homebrew path.
- **Verifier note:** Evidence verified verbatim: Lab 1.1 L447-460 troubleshooting and lab-provision.sh L207-208/L221-224 offer only brew/macOS remediation for the Terraform >=1.8.0 gate, and no week-1 doc states the bash (Linux/macOS/WSL) requirement; repo-wide grep finds zero Linux/Windows install guidance in lab content. One caveat weakens the framing: root README.md's Tools table links the official multi-OS HashiCorp install page, so Linux users are not literally 'stranded' — only the remediation/error-hint paths are macOS-only. The Windows/WSL half holds fully (all lab drivers are bash scripts, requirement unstated). Not covered by any prior-audit item. Recommendation is low-cost and genuinely improves an enterprise course; severity is arguably minor-to-major rather than major.

### F13. Lab 1.7 declares itself 'the final lab of Week 1', says to run cleanup, and routes learners to Week 2 — orphaning Lab 1.8 and the quiz that the README schedules after it
- **Dimension:** crossweek | **Verification:** high
- **File:** `curriculum/week-1-foundations/labs/lab-1.7-multicluster-services.md` L73, 843-859 (vs README.md L32-33, lab-1.8 L55-59, 640)
- **Evidence:** lab-1.7 L73: "This is the final lab of Week 1 -- you can run cleanup after completing it." and L843-859: "## Week 1 Complete!\nCongratulations on completing Week 1! You have: 1. **Lab 1.1**... 7. **Lab 1.7**...\n## Next Week\nProceed to [Week 2: Bare Metal as a Service]" (Lab 1.8 and the quiz are absent from the list). But week-1 README L32-33 schedules "Lab 1.8: Upgrade k0rdent Enterprise | 1.5h" and "Week 1 Assessment" after Lab 1.7, and lab-1.8 L57-59 requires "Completed Labs 1.1-1.7 / Management cluster running with k0rdent Enterprise v1.2.2 / (Optional) Managed cluster from Lab 1.5 still running", ending L640 "Complete the [Week 1 Quiz] to finish Week 1."
- **Fix:** Fix Lab 1.7's ending: next step is Lab 1.8, not Week 2; remove 'final lab of Week 1' and the cleanup go-ahead (Lab 1.8 Part 3 upgrades the managed cluster, and Lab 1.8 L634 forbids lab-destroy.sh). A learner following 1.7 literally tears down then skips both the upgrade lab and the assessment.
- **Verifier note:** Verified verbatim: lab-1.7 L73 says "This is the final lab of Week 1 -- you can run cleanup after completing it" and the file ends with "Week 1 Complete!" (recapping only Labs 1.1-1.7) plus "Next Week -> Week 2"; grep confirms zero mentions of Lab 1.8 or the quiz anywhere in lab-1.7. README L32-33 schedules Lab 1.8 and the Week 1 Assessment after Lab 1.7; lab-1.8 requires the management cluster (managed cluster optional but used hands-on in Part 3), forbids lab-destroy.sh (environment reused through Week 5), and routes to the quiz. Not a duplicate: prior audit C1 (lab-destroy.sh gaps) and C2 (training-baseline-services break) are different defects, and the audit's own C2 text itself calls 1.7 "the final lab" — sharing the mislabel rather than flagging it. The recommendation is a small, clearly correct navigation fix, not pedantry; nuance: the worst cleanup harm is destroying the required management cluster, which strengthens the finding.

### F14. Week 4 (KaaS) is promised everywhere but does not exist, Week 5 hard-requires it, and the gap is explained nowhere
- **Dimension:** crossweek | **Verification:** high
- **File:** `curriculum/week-5-ai-workloads/README.md` L10 (vs README.md L37/L95/L112, overview L27/L78)
- **Evidence:** week-5 README L10: "- Completion of Week 4 (Kubernetes as a Service)" as a prerequisite. Root README.md L95 lists "│   ├── week-4-kaas/            # Cluster API, GPU operator" in the repository structure and L37 "**KaaS** | 4 |"; overview L78 budgets "| 4 | KaaS | 15h | Cluster API, GPU Operator, hosted control planes |". But `find curriculum -maxdepth 2 -type d` shows no week-4 directory at all (weeks jump 3 → 5). The only acknowledgment of missing content is week-5 README L181-184's TODO about a different file: "The full curriculum file does not yet exist at that path" — root README L112 still links [Full Curriculum](curriculum/k0rdent-ai-infrastructure-training-curriculum.md), which is also missing.
- **Fix:** Either add a week-4-kaas stub README stating the content is forthcoming and what to do instead (Week 1 Labs 1.5/1.8 already cover much of CAPI cluster lifecycle), or remove Week 4 from the prerequisite chain, repo-structure diagram, and hour budgets. Also fix or remove the dead Full Curriculum link at README.md L112.
- **Verifier note:** All cited evidence verified verbatim: week-5 README L10 requires Week 4; root README structure lists week-4-kaas/ and L37 KaaS row; overview L27/L78 budget 15h for Week 4; but curriculum/ has no week-4 directory (3 jumps to 5). The only missing-content acknowledgment (week-5 README L181-184 TODO) is about the full-curriculum file, which is also genuinely absent, making README L112's link dead. Corroborating evidence: lab-infrastructure/docs/architecture-design.md L1283 has an unchecked TODO 'Create week4-kaas cluster definitions' — Week 4 was planned, never built. No gap acknowledgment exists anywhere (grep for forthcoming/placeholder/does-not-exist across README and overview). Not covered by the prior Week-1 audit, whose items are all Week 1 lab/theory defects. Recommendation (stub README or remove Week 4 from prereqs/structure/budgets, fix dead link) is a real improvement for a paid course, not pedantry.

### F15. Week 1's two exit links both point to week-2-bmaas/README.md, which does not exist — the Week 1→2 handoff is a dead link
- **Dimension:** crossweek | **Verification:** high
- **File:** `curriculum/week-1-foundations/README.md` L143 (and labs/lab-1.7-multicluster-services.md L859)
- **Evidence:** week-1 README L143: "After completing Week 1, proceed to [Week 2: Bare Metal as a Service](../week-2-bmaas/README.md)" and lab-1.7 L859: "Proceed to [Week 2: Bare Metal as a Service](../../week-2-bmaas/README.md)". `ls curriculum/week-2-bmaas/` returns only "labs" — there is no README.md (week-3-vmaas and week-6-multitenancy likewise have only labs/, no README/intro/quiz, while overview L111-114 promises "Each week includes: **Quiz:** 15-20 multiple choice/short answer questions").
- **Fix:** Create week-2-bmaas/README.md (objectives, schedule, environment provisioning via lab-provision.sh --metal3, quiz) or retarget both links to labs/lab-2.1-explore-sandbox.md. Same for weeks 3 and 6, whose promised weekly quizzes also don't exist.
- **Verifier note:** Verified directly: week-1 README L143 and lab-1.7 L859 both link to week-2-bmaas/README.md; curriculum/week-2-bmaas/ contains only labs/ (no README), so both Week 1 exit links are dead. Weeks 3 and 6 also have only labs/ (no README/quiz) despite overview L111-114 promising a quiz per week. The suggested retarget (labs/lab-2.1-explore-sandbox.md) exists. Prior audit (2026-05-31-week-1-content-audit.md) contains no mention of week-2-bmaas, dead handoff links, or missing weekly READMEs/quizzes — not a duplicate. Recommendation is actionable and material for a paid course.

### F16. Program duration math contradicts itself across four documents (26.5h vs 24.5h for Week 1; ~90h vs ~96+h vs 105.5h total)
- **Dimension:** crossweek | **Verification:** high
- **File:** `curriculum/overview/training-program-overview.md` L15, 75-80 (vs week-1 README L3, root README L27, week-5 README L3-4)
- **Evidence:** week-1 README L3: "**Duration:** 26.5 hours" (schedule table L22-33 sums to 26.5h) vs overview L75: "| 1 | Foundations | 24.5h |". Root README L27: "6 weeks (~15 hours/week, ~90 total hours)" vs overview L15: "6 weeks (~15-25 hours/week, ~96+ total hours)" vs the overview's own weekly table (24.5+15+15+15+21+15 = 105.5h). Week 5 deepens it: week-5 README L3-4 "~50 hours total ... Recommended guided path: ~15-21 hours" vs overview L79 "| 5 | AI Workloads | 21h |".
- **Fix:** Pick one source of truth (the per-week READMEs, since they sum real lab durations) and regenerate the overview and root README tables from it. A paying trainee planning time off against '15 hours/week' meets a 26.5-hour Week 1 in the first table they open.
- **Verifier note:** All quoted figures verified at the cited lines: week-1 README says 26.5h (table sums to exactly 26.5) vs overview L75's 24.5h; root README L27 says ~15h/week ~90h total vs overview L15's 15-25h/week ~96+h vs the overview table's actual 105.5h sum; week-5 README's ~15-21h guided path vs overview's flat 21h also confirmed. Only soft spot: "~96+" technically admits 105.5, but the 26.5-vs-24.5 and ~90-vs-105.5 contradictions are unambiguous. Not a duplicate: the prior audit explicitly DROPPED its "26.5h-vs-15h" item as a stale claude-mem artifact and only retained the within-lab active-vs-wait padding critique; it never compared the week-1 README against the overview or root README, where these contradictions live. Recommendation (per-week READMEs as source of truth, regenerate overview/root tables) is proportionate for a paid course.

### F17. Version pins contradict across overview/root README and the weeks that use them (vLLM v0.11.2 vs v0.14.0; Cluster API v1.11 vs clusterctl v1.12.4)
- **Dimension:** crossweek | **Verification:** high
- **File:** `curriculum/overview/training-program-overview.md` L48, 52 (vs root README L73/L76, week-5 README L131-132, week-1 README L61)
- **Evidence:** overview L52: "| vLLM | v0.11.2 | LLM inference |" and root README L76 same, but week-5 README L131: "| vLLM | Validated example pin: `v0.14.0` |" (and L132 KAI Scheduler `v0.14.0`). overview L48: "| Cluster API | v1.11 | API v1beta2 |" and root README L73 same, but week-1 README L61 pins "| clusterctl | v1.12.4 | Lab 1.1 (auto-installed) |" — clusterctl versions track CAPI minors, so v1.12.4 belongs to CAPI v1.12, not v1.11. The overview's 'validated against' table (L41-53) also omits k0rdent Enterprise itself, the product whose version (1.2.2→1.2.3) drives Week 1.
- **Fix:** Maintain a single versions table (root README) and have overview/week READMEs reference it; update vLLM to the v0.14.0 pin Week 5 actually validates, reconcile CAPI v1.11 vs clusterctl v1.12.4 against what Lab 1.1's installer actually deploys, and add k0rdent Enterprise 1.2.2 to the overview table.
- **Verifier note:** Verified: overview L48/L52 and root README L73/L76 pin Cluster API v1.11 and vLLM v0.11.2, while week-5 README L131-132 and labs 5.5/5.6 pin vllm-openai:v0.14.0 (7+ manifests; lab 5.5 L1555 says 'validated... v0.14.0'), and week-1 README L61 + mgmt-cloud-init.yaml L156 install clusterctl from the cluster-api v1.12.4 release (confirming clusterctl v1.12.4 = CAPI v1.12 artifact). Overview table also omits k0rdent Enterprise (root README has it at v1.2.2). Caveat: the clusterctl/CAPI half is version skew (a v1.12 CLI can manage v1.11 providers), not guaranteed breakage — the vLLM half is the hard contradiction. Not covered by the prior week-1 audit (its version-drift items concern Theory 1.2/1.3 template/CAPA versions, not these tables). Bonus found: lab-5.15 L836 uses vllm-openai:v0.7.3, a third divergent pin.

### F18. Week 3's only lab requires Labs 3.1 and 3.2, which do not exist, and a pre-installed GPU environment whose provisioning path (lab-provision.sh --gpu/--kubevirt) is never taught in any week
- **Dimension:** crossweek | **Verification:** high
- **File:** `curriculum/week-3-vmaas/labs/lab-3.3-ai-vm-8gpu.md` L11-30
- **Evidence:** lab-3.3 L13-14: "- Completed Lab 3.1 (KubeVirt deployed)\n- Completed Lab 3.2 (GPU Passthrough configured)" — `ls curriculum/week-3-vmaas/labs/` shows only "CLAUDE.md  lab-3.3-ai-vm-8gpu.md". L20-30: "**Cloud Instance:** AWS P4d.24xlarge ... **Pre-installed:** k0rdent management, KubeVirt v1.6.3 operator, GPU Operator v25.10.0" — but no lab in Weeks 1-3 ever shows how to get this environment, even though lab-infrastructure supports it (lab-provision.sh L178-179: "--gpu  Enable GPU lab environment / --metal3  Enable Metal3 dev environment"; L197: "$0 john-doe --metal3 --kubevirt"). Week 1's Lab 1.1 teaches only the plain invocation.
- **Fix:** Author Labs 3.1/3.2 (or renumber 3.3) and open Week 2/3 with an explicit provisioning step using the existing --metal3/--gpu/--kubevirt flags, bridging from Week 1's environment. Right now the trainee's path dead-ends at prerequisites that reference files that were never written.
- **Verifier note:** Verified exactly: lab-3.3 L13-14 requires Labs 3.1/3.2 which never existed in any git commit (week-3-vmaas/labs/ contains only CLAUDE.md + lab-3.3). lab-provision.sh L178-180 defines --gpu/--metal3/--kubevirt with examples at L196-197, but curriculum-wide grep shows only Lab 1.1's plain --region/--auto-approve invocation is ever taught; no week documents the GPU lab provisioning path. Finding is actually understated: terraform/modules/gpu-lab/main.tf L140 explicitly provisions the p4d.24xlarge "for Lab 3.3", proving the infra-to-curriculum bridge was intended but never written, and Week 5's labs/README.md L71 documents its own distinct GPU path ("No special --gpu flag is needed"), leaving Week 3 the only GPU-dependent week with no provisioning instructions. Not covered by the Week 1-scoped prior audit (closest item, Lab 1.1 wrapper-script opacity, is different substance). Recommendation is actionable; severity major is appropriate.

### F19. Lab 1.6 Grafana SSH-tunnel command uses the author's leftover key names and the wrong bastion user — fails copy-paste for every student
- **Dimension:** drilldown | **Verification:** high
- **File:** `curriculum/week-1-foundations/labs/lab-1.6-deploy-kof.md` L748-757
- **Evidence:** L749-754: "ssh ... -o ProxyCommand=\"ssh -i ./config/keys/bastion.pem -o StrictHostKeyChecking=no -W %h:%p ubuntu@<BASTION_IP>\" \\\n    -i ./config/keys/lab-validation-k0rdent.pem \\\n    -L 3000:localhost:3000 \\\n    ubuntu@<MANAGEMENT_NODE_IP>". Actual key files are per-student: scripts/lab-connect.sh L72 `key_file="$CONFIG_DIR/keys/${identifier}-k0rdent.pem"` and L98 `"$CONFIG_DIR/keys/${identifier}-bastion.pem"` — there is no `bastion.pem`, and `lab-validation-k0rdent.pem` is the author's engineer-id, not the student's. The bastion login user is also `ec2-user`, not `ubuntu`: lab-connect.sh L139 "ec2-user@${bastion}" and lab-1.3 L218 uses "ec2-user@<bastion-ip>". Remote students cannot reach Grafana — a core Lab 1.6 deliverable.
- **Fix:** Rewrite as `ssh -i ./config/keys/<your-id>-bastion.pem ... ec2-user@<BASTION_IP>` proxy with `-i ./config/keys/<your-id>-k0rdent.pem`, or better, add a `--tunnel 3000` example using lab-connect.sh (it already supports -L tunneling via connect_ssh, L146). Add the same fix anywhere the tunnel pattern recurs.
- **Verifier note:** Verified on all points. lab-1.6-deploy-kof.md L749-754 hardcodes ./config/keys/bastion.pem and ./config/keys/lab-validation-k0rdent.pem; the provisioning tooling (lab-infrastructure/scripts/lab-connect.sh L72/L98) only ever creates ${identifier}-k0rdent.pem and ${identifier}-bastion.pem, and 'lab-validation' appears nowhere else in the curriculum — it is the author's leftover validation identifier. The bastion user is provably wrong: terraform/modules/bastion/main.tf L47-53 uses an AL2023 AMI (default user ec2-user), corroborated by bastion/outputs.tf L36, lab-connect.sh L139/165/304/336, and lab-1.3 L218 — so ubuntu@<BASTION_IP> in the ProxyCommand fails publickey auth and the tunnel never establishes, blocking Grafana (core Lab 1.6 Part 4 deliverable) for any student working remotely. Recommendation is actionable: lab-connect.sh already implements -L tunneling via --tunnel <local:remote> (L142-146), so the example should be ./lab-connect.sh <id> --tunnel 3000:3000 (note: finding's '--tunnel 3000' shorthand should be '3000:3000'). Not covered by any prior-audit item; the closest (Grafana binds 0.0.0.0, Lab 1.6 difficulty cliff/no-cleanup) address different defects. Minor caveat: only the remote-access path breaks; students running everything on the management node are unaffected, but 'major' is still justified for a paid course.

### F20. Lab 1.6 expected output 'SERVICES 4/4 (nginx + cert-manager + kof-operators + kof-collectors)' is impossible at that point — nginx is not deployed until Lab 1.7
- **Dimension:** drilldown | **Verification:** high
- **File:** `curriculum/week-1-foundations/labs/lab-1.6-deploy-kof.md` L453-457
- **Evidence:** L455-457: "kubectl get clusterdeployment managed-cluster-01 -n kcm-system\n\n# Should show: SERVICES 4/4 (nginx + cert-manager + kof-operators + kof-collectors)". The lab's own Step 3 (L443-446) says kof-child deploys exactly three services ("cert-manager, kof-operators, kof-collectors"); ingress-nginx is first deployed in Lab 1.7. A student following the labs in order sees 3/3 and concludes the deployment is incomplete. (Leftover from an author validation run where Lab 1.7's nginx already existed.)
- **Fix:** Change to "SERVICES 3/3 (cert-manager + kof-operators + kof-collectors)" and add a note that the count grows in Lab 1.7.
- **Verifier note:** Verified against the file and upstream sources. lab-1.6-deploy-kof.md L457 expects "SERVICES 4/4 (nginx + ...)" but the kof-child chart v1.5.0 (exact version installed at L438-440) defines an MCS with exactly 3 services (cert-manager, kof-operators, kof-collectors — confirmed in k0rdent/kof v1.5.0 charts/kof-child/templates/child-multi-cluster-service.yaml), Lab 1.5's ClusterDeployment has no serviceSpec.services, and ingress-nginx first appears in Lab 1.7 (ingress-services MCS, template ingress-nginx-4-11-0). kcm's SERVICES printer column (ServicesInReadyState condition) counts MCS-delivered services, so a student in lab order sees 3/3 and wrongly concludes failure. Not covered by prior audit (its Lab 1.6 items are difficulty cliff/Core path table and missing cleanup/cost; C2 is Lab 1.7's training-baseline-services). Recommendation (3/3 + note that count grows in Lab 1.7) is correct. Only caveats: the "leftover from author validation run" origin is speculation (4/4+nginx more likely copied from upstream KOF regional-cluster verification), and severity could arguably be minor since cluster state is actually healthy.

### F21. Lab 1.7 expected outputs describe a two-cluster fleet (managed-cluster-02) that is never created, and ignore the 3 KOF services already on managed-cluster-01 from Lab 1.6
- **Dimension:** drilldown | **Verification:** high
- **File:** `curriculum/week-1-foundations/labs/lab-1.7-multicluster-services.md` L311-314, 418, 462-464, 487-491
- **Evidence:** L311-313 only ever comments out the second cluster: "# Cluster-02 gets only environment label (no ingress)\n# kubectl label clusterdeployment managed-cluster-02 ...". Yet L418: "This deploys cert-manager and kyverno to **every cluster** with `environment: training` — both managed-cluster-01 and managed-cluster-02."; L462-464: "Expected:\n- `baseline-services` → `2/2` clusters (both have `environment: training`)"; L489-491: "# managed-cluster-01 should show SERVICES 3/3 ...\n# managed-cluster-02 should show SERVICES 2/2 (cert-manager + kyverno only)". A student has exactly one cluster (1/1), and after Lab 1.6's kof-child added cert-manager+kof-operators+kof-collectors, managed-cluster-01's SERVICES count would be 6, not 3/3 — every verification number on this page mismatches reality.
- **Fix:** Rewrite expected outputs for the actual single-cluster state (1/1 matched; SERVICES count including KOF services if Lab 1.6 Part 3 was done), and either provision a real second cluster as an optional step or consistently frame managed-cluster-02 as hypothetical ("if you had a second cluster...").
- **Verifier note:** Verified. All cited lines exist verbatim in lab-1.7-multicluster-services.md (L311-313 commented-out cluster-02 labeling, L418 'both managed-cluster-01 and managed-cluster-02', L462-464 '2/2 clusters', L489-490 'SERVICES 3/3' / '2/2'). Repo-wide grep confirms managed-cluster-02 is never created by any lab or script; Lab 1.7 prerequisites reference only 'the managed cluster from Lab 1.5' (singular). The KOF half also holds: Lab 1.6 Part 3 Option 1 (Recommended, and inside the Core path Parts 1-4,7) installs kof-child which deploys cert-manager+kof-operators+kof-collectors to managed-cluster-01 via MCS, and Lab 1.6 L457 itself expects 'SERVICES 4/4' on that ClusterDeployment — directly contradicting Lab 1.7's '3/3' one lab later. Minor caveat: the finding's exact count of 6 is approximate (cert-manager is deployed by both the kof-child MCS and Lab 1.7's baseline-services, so it may collide on release name rather than add; Lab 1.6's '4/4' also includes a questionable 'nginx' entry), but the central claim — every verification number in Parts 3-5 mismatches single-cluster reality — is correct. Not a duplicate: prior C2 covers Lab 1.7's training-baseline-services references (L575/743/746) and merge-patch behavior, a different defect at different lines; no prior item mentions the phantom second cluster or expected-output counts. Severity 'major' is appropriate and the recommendation is actionable.

### F22. Lab 1.7 baseline-services installs cert-manager (with CRDs) onto a cluster where Lab 1.6's kof-child already deployed cert-manager — Helm CRD-ownership conflict blocks the whole MCS
- **Dimension:** drilldown | **Verification:** high
- **File:** `curriculum/week-1-foundations/labs/lab-1.7-multicluster-services.md` L389-420 (and lab-1.6 L443-446, L470-472)
- **Evidence:** Lab 1.6 L443-446: "This will automatically deploy:\n- **cert-manager** - For TLS certificates" to managed-cluster-01 (expected pods L470-472: "cert-manager-xxx (3 pods)" on the child). Lab 1.7 then targets the same cluster (selector `environment: training`) with a second cert-manager release: L400-405 "- template: cert-manager-1-16-2\n      name: cert-manager\n      namespace: cert-manager\n      values: |\n        cert-manager:\n          crds:\n            enabled: true". Cluster-scoped cert-manager CRDs/webhooks already owned by the kof-child release cause the second Helm install to fail with invalid-ownership errors (or two controllers racing), and per the lab's own L420 note ("services in a MultiClusterService deploy **sequentially**, a failing cert-manager also blocks kyverno"), the failure cascades to kyverno. No lab acknowledges this interaction.
- **Fix:** Either drop cert-manager from baseline-services when Lab 1.6 Part 3 was completed (add an explicit branch: 'If you deployed kof-child in Lab 1.6, cert-manager already exists — deploy only kyverno here'), or have Lab 1.6's child deployment section deferred until after Lab 1.7, or document removing the kof-child MCS first.
- **Verifier note:** Verified against both lab files and the actual kof-child 1.5.0 chart (pulled from oci://ghcr.io/k0rdent/kof/charts/kof-child). Lab 1.6 Part 3 Option 1 (Recommended, and inside the Lab Path table's Core 'Parts 1-4, 7') installs kof-child, whose templates/child-multi-cluster-service.yaml deploys Helm release 'cert-manager' (template cert-manager-v1-16-4, crds.enabled=true) into namespace 'kof' on managed-cluster-01 (cert-manager.enabled=true by default in values.yaml). Lab 1.7 then labels the same cluster environment=training and baseline-services installs a second release 'cert-manager' in namespace 'cert-manager' (jetstack 1.16.2 via catalog wrapper, CRDs templated). Cluster-scoped CRDs/ClusterRoles/webhooks carry meta.helm.sh/release-namespace: kof, so the second install fails Helm 3's ownership check ('invalid ownership metadata'); Sveltos uses the Helm SDK so the failure surfaces in ClusterSummary, and per Lab 1.7's own L420 sequential-deployment note, kyverno is blocked too. Sveltos conflict registration doesn't prevent it (keyed on release ns+name, which differ). Corroborating drift: Lab 1.7 Part 5 expects 'SERVICES 3/3' on managed-cluster-01 while Lab 1.6 left it at 'SERVICES 4/4'. Neither lab acknowledges the interaction. Not covered by prior audit (C2 is the 'training-baseline-services' name/merge-patch issue; Lab 1.6 items are path-table/cleanup/difficulty). Minor caveats: 'two controllers racing' alternative is unlikely (Helm fails before deploying), and only the baseline-services MCS is blocked — ingress-services is separate. Severity 'major' is appropriate; recommendation (branch note or drop cert-manager from baseline-services when kof-child exists) is a genuine fix, not pedantry.

### F23. Lab 1.7 declares 'Week 1 Complete' and routes to Week 2, orphaning Lab 1.8 and the quiz — and invites cleanup of the cluster Lab 1.8 Part 3 uses
- **Dimension:** drilldown | **Verification:** high
- **File:** `curriculum/week-1-foundations/labs/lab-1.7-multicluster-services.md` L73, 843-859
- **Evidence:** L73: "This is the final lab of Week 1 -- you can run cleanup after completing it." L843: "## Week 1 Complete!" and L857-859: "## Next Week\n\nProceed to [Week 2: Bare Metal as a Service](../../week-2-bmaas/README.md)". But README.md L31-33 schedules "Lab 1.8: Upgrade k0rdent Enterprise | 1.5h" and "Week 1 Assessment | 0.5h | Quiz" after Lab 1.7 on Day 5, and lab-1.8 L55-57 requires "Completed Labs 1.1-1.7 ... (Optional) Managed cluster from Lab 1.5 still running". A sequential learner skips Lab 1.8 and the quiz entirely, or deletes the managed cluster that Lab 1.8 Part 3 (managed-cluster upgrade) needs.
- **Fix:** Fix Lab 1.7's footer: 'Next Lab: Lab 1.8 - Upgrade k0rdent Enterprise', move the 'Week 1 Complete' celebration and Week-2 link to the end of Lab 1.8 (which already links the quiz), and change L73 to defer cleanup until after Lab 1.8.
- **Verifier note:** Verified verbatim in curriculum/week-1-foundations/labs/lab-1.7-multicluster-services.md: L73 "This is the final lab of Week 1 -- you can run cleanup after completing it", L843 "## Week 1 Complete!" (lists only 1.1-1.7), L857-859 "Next Week -> Week 2"; no Next Lab footer, breaking the chain pattern used by labs 1.5/1.6 ("Next Lab" footers). README.md L31-33 schedules Lab 1.8 + Week 1 Assessment after Lab 1.7; lab-1.8 requires "Completed Labs 1.1-1.7", its Part 3 upgrades the Lab 1.5 managed cluster (prereq labeled "(Optional)" but Part 3 has no skip guidance), and only lab-1.8's footer links the quiz. The finding is actually understated: lab-1.8's "Preserving Your Environment" section says "Do NOT run lab-destroy.sh after completing Week 1" (mgmt cluster reused through Week 5), so L73's cleanup invite contradicts curriculum policy beyond just Lab 1.8 Part 3. Not in the prior audit: C1 is about lab-destroy.sh orphaning cloud resources (cost), not the 1.7->1.8 navigation break; no audit line mentions the "Week 1 Complete"/Week-2 footer. Recommendation (footer to Lab 1.8, move celebration/Week-2 link to 1.8, defer cleanup) is minimal and correct. Minor caveat: lab-1.8 lists the managed cluster as optional, so Part 3 is degraded rather than hard-blocked.

### F24. k0rdent admin UI is published to the public internet over plaintext HTTP (port 80, no TLS listener), and bastion SSH defaults to 0.0.0.0/0 — every student's management plane login transits cleartext
- **Dimension:** floor | **Verification:** high
- **File:** `lab-infrastructure/terraform/modules/k0rdent-mgmt/templates/mgmt-cloud-init.yaml` L343-352 (plus lab-provision.sh 155/360-365, lab-connect.sh 315-317, networking/main.tf 86-89, student-lab/variables.tf 32-36)
- **Evidence:** mgmt-cloud-init.yaml L343-352 defines 'kind: Gateway ... listeners: - name: http / protocol: HTTP / port: 80' with no TLS listener; networking/main.tf L86-89 tags public subnets '"kubernetes.io/role/elb" = "1"', so the CCM provisions an internet-facing load balancer. lab-provision.sh L155 'echo "http://${gw_addr}"' and L362-364 print 'URL: ... http://' plus 'Password: ${ui_password}'; lab-connect.sh L315-317 prints 'URL: http://${GW_ADDR} ... Password: ${UI_PASSWORD}'. Students log into the admin console of the platform that holds their AWS credentials over unencrypted HTTP from home/corporate networks, and the URL is guessable/scannable (public ELB DNS, port 80, no IP allowlist). Separately, student-lab/variables.tf L32-36: 'variable "allowed_ssh_cidrs" ... default = ["0.0.0.0/0"]' — bastion SSH is world-open by default and no student-facing doc ever mentions setting it (grep: allowed_ssh_cidrs appears only in lab-infrastructure/docs/plans/2026-02-20-per-student-infrastructure.md).
- **Fix:** Minimum fix: restrict the Gateway LB to the student's IP (loadBalancerSourceRanges via Envoy Gateway / Service annotation, or reuse allowed_ssh_cidrs), and have Lab 1.1 prompt students to pass their public IP for allowed_ssh_cidrs. Better: add an HTTPS listener (self-signed or cert-manager) and a boxed 'training-only: this UI is plaintext HTTP' warning consistent with the course's Lab 1.10/TRAP 22 secrets posture; corporate networks that block or MITM port-80 will also break the UI silently today.
- **Verifier note:** All cited evidence verified verbatim. mgmt-cloud-init.yaml L342-352 creates a Gateway with a single HTTP:80 listener (no TLS); the AWS CCM is installed explicitly to provision the LB (L243-258); public subnets carry kubernetes.io/role/elb=1 tags (networking/main.tf L85-89); no internal-LB annotation or loadBalancerSourceRanges exists anywhere in the repo, so the ELB is internet-facing and world-open. Finding is actually UNDERSTATED: the UI uses HTTP basic auth (cloud-init L298 `k0rdent-ui.auth.basic.password`), so the admin credential transits base64-cleartext on every request, not just at login. lab-provision.sh L155/L360-365 and lab-connect.sh L315-317 print http:// URL + password as cited. allowed_ssh_cidrs defaults to ["0.0.0.0/0"] (terraform/environments/student-lab/variables.tf L32-36 — finding's path omits 'environments/'), feeds bastion SG port-22 ingress (networking/main.tf L210-214), and is never mentioned in student-facing docs. Not covered by any prior-audit item (closest are plaintext IAM-keys posture and Grafana 0.0.0.0 binding, both different substance). Recommendation (source-range restriction and/or TLS + training-only warning) is proportionate and valuable; corporate port-80 blocking/MITM is a real failure mode for enterprise students. Severity 'major' is appropriate given random per-student password and TTL'd lab scope.

### F25. No Terraform state locking + engineer-id used as the only namespace: two students who pick the same identifier in a shared account silently share one S3 state file and can corrupt or hijack each other's environment
- **Dimension:** floor | **Verification:** high
- **File:** `lab-infrastructure/terraform/environments/student-lab/main.tf` L7 (plus scripts/lab-provision.sh 245, 286-289, 433-436)
- **Evidence:** main.tf L7: 'backend "s3" {}' and lab-provision.sh L286-289: 'terraform init -reconfigure -backend-config="bucket=${bucket}" -backend-config="key=terraform.tfstate" -backend-config="region=${REGION}"' — no dynamodb_table and no use_lockfile, so there is zero state locking. The bucket name is purely identifier-derived (L245: 'echo "k0rdent-lab-${engineer_id}-${account_id}-${REGION}"'), and the script accepts any free-text name with no uniqueness check (L433 only validates non-empty: 'Your name/identifier is required'). In a cohort sharing one AWS account, two students who both run './lab-provision.sh john --region us-east-1' (docs repeatedly suggest 'john-doe'/'engineer-01' style ids) land in the SAME bucket and state: the second apply plans against the first student's resources, concurrent applies interleave writes with no lock, and lab-destroy.sh would tear down someone else's lab. (Corroborated by qa_report.md STATE-2 'Concurrent provisioning with same engineer_id causes state lock | High | Undocumented' and STATE-8 'No DynamoDB lock table - concurrent applies can corrupt state'.)
- **Fix:** Enable S3 native locking (-backend-config="use_lockfile=true", Terraform >= 1.10 — the script already requires >= 1.8; bump to 1.10) or a DynamoDB lock table. In lab-provision.sh, fail fast if the bucket exists but was created by a different caller identity (compare aws sts get-caller-identity Arn against a marker object), and tell instructors to assign unique IDs (e.g., include initials + random suffix).
- **Verifier note:** Verified line-by-line: main.tf L7 'backend "s3" {}'; lab-provision.sh L286-289 inits with only bucket/key/region (no dynamodb_table/use_lockfile — also confirmed null in the cached .terraform backend state); L245 bucket name is purely id+account+region with fixed key terraform.tfstate; L433-436 validates only non-emptiness; ensure_student_bucket (L251-253) reuses an existing bucket with no ownership check; lab-destroy.sh derives the same bucket and runs terraform destroy against it; README.md L22 suggests generic ids like 'john-doe'. So id collision in a shared account means shared state, key exfiltration via terraform output, lock-free concurrent applies, and cross-student destroy — all technically accurate Terraform S3-backend behavior. use_lockfile needs TF >= 1.10 (script requires 1.8), so the recommended version bump is apt. Bonus: troubleshooting.md L52-61 documents an 'Error acquiring state lock' that cannot occur with this lockless config. Not covered by the prior audit (C1 concerns ClusterDeployment orphans, not state). Two caveats: finding's file path omits the lab-infrastructure/ prefix (line numbers correct), and its cited corroboration 'qa_report.md STATE-2/STATE-8' does not exist in the repo — evidence stands on direct source verification alone.

### F26. Windows students have no path into the course: every entry point is bash, install guidance is macOS/brew-only, and no doc states an OS requirement or WSL2 prerequisite
- **Dimension:** floor | **Verification:** high
- **File:** `curriculum/week-1-foundations/README.md` L123-127 (plus labs/lab-1.1-provision-k0rdent.md 450, scripts/lab-provision.sh 1, 207-209)
- **Evidence:** README.md Prerequisites (L123-127) lists only: '- AWS Account with appropriate permissions / - Basic Kubernetes knowledge / - Familiarity with Terraform' — no OS, no tool list, no shell requirement. The entire Week 1 on-ramp is '#!/bin/bash' scripts (lab-provision.sh L1) full of bash-isms (set -euo pipefail, BASH_SOURCE, eval ssh with ProxyCommand), every lab uses 'cat << EOF' heredocs and 'eval "$(aws configure export-credentials --format env ...)"', and the only platform-specific install help in all of Week 1 is macOS: lab-1.1 L450 '# Install newer Terraform (macOS)' and lab-provision.sh L208 'brew install hashicorp/tap/terraform   # macOS'. grep -i 'windows|wsl' across curriculum/week-1-foundations returns nothing. An enterprise trainee on a Windows laptop (a large fraction of any paying cohort) opens PowerShell, cannot run lab-provision.sh, and has zero guidance; even Git Bash users hit chmod-600 pem-permission and ProxyCommand quirks.
- **Fix:** Add to README Prerequisites: 'Supported workstation: macOS or Linux; Windows users MUST use WSL2 (Ubuntu) — native PowerShell/Git Bash are not supported', plus a short WSL2 setup note (install terraform/aws-cli/jq inside WSL, keep the repo on the Linux filesystem for ssh key permissions). Add a uname/OSTYPE guard in lab-provision.sh that prints this message on msys/cygwin.
- **Verifier note:** All evidence verified. Week 1 README Prerequisites contains only the three quoted bullets (no OS/tools/shell). lab-provision.sh (actual path: lab-infrastructure/scripts/lab-provision.sh, not scripts/) is bash-only (#!/bin/bash, set -euo pipefail, BASH_SOURCE, ssh ProxyCommand) and its check_prerequisites() offers only macOS brew install guidance (L208, L221-223); aws CLI check has no install hint. Lab 1.1 ~L450 troubleshooting is macOS/brew-only; eval "$(aws configure export-credentials ...)" confirmed in labs 1.1/1.3. Zero Windows/WSL/PowerShell mentions in week-1 or lab-infrastructure; the only curriculum-wide Windows mention is Lab 5.4's Run:AI CLI download menu. Root README's "Familiarity with Linux system administration" is an experience prereq, not a workstation OS statement, so it does not refute the claim. Not covered by the prior audit (grep of the audit doc for windows/wsl/powershell/workstation returns nothing). Recommendation is practical and low-cost for an enterprise cohort where Windows laptops are common; Lab 1.1 is a hard day-1 blocker without it. Minor correction for the report: cite lab-infrastructure/scripts/lab-provision.sh.

### F27. 'Resuming This Lab' blocks restore the SSH session but not the session state it depends on: Lab 1.5's heredocs silently interpolate empty ${AWS_REGION}/${TEMPLATE_NAME} after a reconnect, and no resume block tells SSO users to refresh the (certainly expired) CAPA credentials
- **Dimension:** floor | **Verification:** high
- **File:** `curriculum/week-1-foundations/labs/lab-1.5-provision-managed-cluster.md` L74-89 vs 139-141, 178, 180-211 (plus lab-1.6 L76-90, lab-1.8 L59-67)
- **Evidence:** Lab 1.5's resume block (L74-89) is only: './scripts/lab-connect.sh <your-engineer-id>' + 'kubectl get nodes' + 'kubectl get clusterdeployment -n kcm-system'. But Part 1 sets 'export AWS_REGION="eu-west-1"  # <-- adjust to YOUR region' (L141) and Part 2 sets 'TEMPLATE_NAME="aws-standalone-cp-1-0-20"' (L178) in that SSH session, then the unquoted heredoc 'cat << EOF > /tmp/managed-cluster-01.yaml' (L180) interpolates 'template: ${TEMPLATE_NAME}' (L190) and 'region: ${AWS_REGION}' (L195). A student who drops the session between Part 1 and Part 2 (or returns 3 days later and re-runs the heredoc while troubleshooting) renders 'template:' and 'region:' empty — apply is rejected with an opaque CRD validation error, or worse produces a misconfigured manifest; later commands 'aws ec2 ... --region $AWS_REGION' (L296, L593) fail with 'You must specify a region'. Additionally, Lab 1.6's (L76-90) and Lab 1.8's (L59-67) resume blocks never mention credentials, yet for SSO users returning after 'the next day' the AWSClusterStaticIdentity SessionToken (max 12h) is guaranteed expired, so CAPA is in AuthFailure before they start — the fix (scripts/lab-refresh-creds.sh) exists but is referenced only once, in lab-1.3 L234. (Token-expiry-on-long-gap corroborated by qa_report.md TOK-1..TOK-5, 'No guidance on refreshing credentials mid-lab | Undocumented'.)
- **Fix:** Make every 'Resuming This Lab' block re-establish state: (1) 're-export AWS_REGION=<your region> and TEMPLATE_NAME (kubectl get clustertemplates ...)' in Lab 1.5; (2) add 'SSO users: run ./scripts/lab-refresh-creds.sh <your-name> before continuing' to Labs 1.5-1.8 resume blocks; (3) add a guard line before each interpolating heredoc: ': "${AWS_REGION:?export AWS_REGION first}"'.
- **Verifier note:** Verified against the repo; the finding holds. (1) Evidence is accurate: lab-1.5 L74-89 resume block is only lab-connect + kubectl checks; L141 'export AWS_REGION="eu-west-1"', L178 TEMPLATE_NAME=..., L180-211 unquoted 'cat << EOF' heredoc interpolating 'template: ${TEMPLATE_NAME}' (L190) and 'region: ${AWS_REGION}' (L195); '--region $AWS_REGION' reused at L296 and L593. Crucially, the lab VM's persisted environment (/etc/profile.d/k0rdent-lab.sh in lab-infrastructure/terraform/modules/k0rdent-mgmt/templates/mgmt-cloud-init.yaml L536+) exports only PATH/KUBECONFIG/aliases — NOT AWS_REGION — so a reconnect genuinely loses the variable and the heredoc renders empty fields. Lab 1.6 (L76-90), 1.7 (L75+), 1.8 (L59-66) resume blocks all say 'returning the next day' yet never mention credentials; scripts/lab-refresh-creds.sh exists and within week 1 is referenced only at lab-1.3 L234. The script's own code and week-5 lab-5.1 L1053 confirm the authors know expired STS tokens put CAPA into AuthFailure/RequestExpired and that the secret+CAPA restart is the fix — week-1 resume blocks just never route students there. TOK-1/TOK-5 corroboration exists in .auto-claude/specs/012-*/build-progress.txt L1250/1254. (2) Technically correct: unquoted heredocs expand unset vars to empty; SSO/STS session tokens max ~12h so next-day return guarantees expiry for SSO users; CAPA caches credentials from aws-cluster-identity-secret. Two minor overstatements that don't change the verdict: (a) in the drop-between-Part-1-and-Part-2 scenario only AWS_REGION is certainly empty (TEMPLATE_NAME at L178 immediately precedes the heredoc and would be re-run); TEMPLATE_NAME loss needs the also-named 're-run heredoc days later while troubleshooting' path; (b) the exact CLI error is 'argument --region: expected one argument' (empty unquoted word is dropped) rather than 'You must specify a region' — either way the command fails. (3) Not a duplicate: prior audit's C5 is the opposite defect (Azure single-quoted heredoc blocking expansion at L533); the 'hardcoded --region us-east-1 in Lab 1.3' and 'plaintext IAM keys posture' items are distinct; nothing in the prior list addresses resume-block state restoration or mid-lab credential refresh guidance. (4) Recommendation is proportionate and valuable for a multi-day enterprise course: re-export lines in resume blocks, a refresh-creds pointer for SSO users, and ': "${AWS_REGION:?...}"' guards are cheap, targeted fixes for a guaranteed-stranding scenario. Severity 'major' is appropriate.

### F28. jq is a hard runtime dependency of all four lab scripts but is never checked by check_prerequisites and is not preinstalled on macOS or Ubuntu — failure surfaces as the misleading error 'Is the environment provisioned?'
- **Dimension:** floor | **Verification:** high
- **File:** `lab-infrastructure/scripts/lab-connect.sh` L60-61, 86 (plus lab-provision.sh 203-239 vs 317, lab-refresh-creds.sh 93-94)
- **Evidence:** Harvested from .auto-claude/specs/012-review-week1-for-scenarios-where-it-won-t-work-for/qa_report.md Top-10 #10 ('jq Dependency (Scripts) - Both infrastructure scripts require jq but don't verify installation') and re-verified against current code: lab-provision.sh check_prerequisites (L203-239) tests only terraform, aws, and sts get-caller-identity — never jq (nor ssh/scp). Yet lab-provision.sh L317 pipes 'terraform output -json mgmt_node_ids ... | jq -r' (failure is swallowed by '|| echo ""', silently skipping the instance-readiness wait), and lab-connect.sh's entire state access is L60-61: 'aws s3 cp "s3://${bucket}/terraform.tfstate" - 2>/dev/null | jq -r ...' — with jq missing every lookup returns empty and the student gets L86 'Could not retrieve SSH key. Is the environment provisioned?' minutes after a successful provision, pointing them at the wrong cause. jq ships by default on neither macOS nor minimal Ubuntu.
- **Fix:** Add 'command -v jq' (and 'command -v ssh') to check_prerequisites in lab-provision.sh, and a one-line guard at the top of lab-connect.sh / lab-refresh-creds.sh / lab-destroy.sh: 'command -v jq >/dev/null || { log_error "jq is required: brew install jq / apt install jq"; exit 1; }'. List jq in the README prerequisites tool list.
- **Verifier note:** Verified real with two corrections. Code evidence holds exactly as cited: check_prerequisites in lab-infrastructure/scripts/lab-provision.sh (L203-239) checks only terraform/aws/credentials; zero jq guards exist anywhere in lab-infrastructure/; jq is a hard dependency of lab-connect.sh L60-61, lab-destroy.sh L76, lab-refresh-creds.sh L94, lab-status.sh (10+ uses), and lab-provision.sh L317 (failure swallowed by '|| echo ""', silently skipping wait_for_instance); README prerequisites omit jq. Corrections: (1) the macOS claim is wrong — Apple bundles /usr/bin/jq since macOS 15 Sequoia (verified jq-1.7.1-apple locally), so only Ubuntu/Debian/WSL students and Macs on <=14 are exposed, making 'major' slightly inflated; (2) the actual symptom is worse than claimed — get_ssh_key is always called via command substitution under set -euo pipefail, so the L86 log_error (written to stdout) is captured into KEY_FILE and the script exits silently with no message at all, rather than showing the misleading 'Is the environment provisioned?' text. Also, the cited provenance is wrong: qa_report.md in .auto-claude/specs/012-* contains no jq mention. Not covered by any prior-audit item (C1 destroy gaps and production-readiness.sh are different substance). The recommended command -v jq guard + README bullet is a cheap, genuine fix for an enterprise course — jq-dependence in lab-destroy.sh in particular means cleanup can fail confusingly, with cost implications.

### F29. SSO users' exported env credentials can expire mid-'terraform apply' in Lab 1.1 (~15-20 min apply + cloud-init wait), failing the very first lab with partial state; the lab's only note is reactive, not preventive
- **Dimension:** floor | **Verification:** medium
- **File:** `curriculum/week-1-foundations/labs/lab-1.1-provision-k0rdent.md` L465-485
- **Evidence:** Harvested from qa_report.md SSO-2 ('SSO token expiration during long provisioning (20+ min) | High | Partial') and TOK-2, re-verified: lab-1.1 L472-482 gives the SSO workaround 'aws sso login ... eval "$(aws configure export-credentials --format env ...)"' then './scripts/lab-provision.sh <your-engineer-id> --region <your-region> --auto-approve', and L485 only says: '> **Note:** The exported credentials are temporary session tokens. If your session expires, run the `aws sso login` and `eval` commands again.' Nothing warns that the apply itself takes 15-20+ minutes (lab-provision.sh L81 budgets 1200s just for cloud-init) and that org-configured 15-60-minute session durations are common — expiry mid-apply aborts Terraform with ExpiredToken, leaving partially-created billable resources (VPC/NAT/instances) and a state file the student must know to re-apply over. Most enterprise trainees are SSO-only, so this is the dominant first-day failure mode.
- **Fix:** In the Lab 1.1 SSO section, add: 'Before running lab-provision.sh, start a FRESH SSO session (run aws sso login immediately before) and confirm your session duration is >= 1 hour (ask your AWS admin). If apply fails with ExpiredToken, re-run the login + eval commands and re-run the same lab-provision.sh command — Terraform resumes from state.' Optionally have lab-provision.sh print remaining-credential-lifetime via 'aws configure export-credentials' Expiration before applying.
- **Verifier note:** Core gap verified: lab-1.1 L472-485 has only a reactive expiry note for the SSO static-credential workaround; no preventive fresh-login guidance and no 're-run resumes from Terraform state' recovery instruction, and lab-provision.sh L325-327 will hard-fail key saves under set -e if creds expire post-apply. Not covered by the prior audit (C1/quiz items mention SSO ExpiredToken only for destroy-path and capa-controller-manager). However the finding overstates the mechanism: the 1200s cloud-init wait (L81) is SSH-only and needs no AWS creds, the base-lab apply is ~5-8 min (no provisioners in the enabled modules) so the credential-requiring window is ~10-15 min, and IAM Identity Center permission sets have a 1-hour minimum — '15-60-minute org-configured sessions' is wrong; the realistic trigger is aged cached role credentials (login in Part 1, provision in Part 3, export-credentials reuses the cache). Severity is closer to minor-major boundary; the recommended preventive note is cheap and genuinely valuable for SSO-only enterprise trainees.

### F30. Bastion SSH is open to 0.0.0.0/0 by default and the documented ALLOWED_SSH_CIDRS restriction is wired to nothing
- **Dimension:** infra | **Verification:** high
- **File:** `lab-infrastructure/terraform/environments/student-lab/variables.tf` L32-36; lab-provision.sh 306-313; config/lab-config.env.example 7-8
- **Evidence:** variables.tf: `variable "allowed_ssh_cidrs" { description = "CIDR blocks allowed to SSH to bastion" ... default = ["0.0.0.0/0"] }` feeds the bastion SG ingress (networking/main.tf 208-214: `from_port = 22 ... cidr_blocks = var.allowed_ssh_cidrs`). lab-provision.sh's terraform apply (lines 306-313) passes engineer_id/region/student_bucket/node_count/enable_* only — never allowed_ssh_cidrs. config/lab-config.env.example line 7-8 instructs: `# SSH Access - IMPORTANT: Restrict to your IP ranges` / `export ALLOWED_SSH_CIDRS='["YOUR_IP/32"]'`, but a repo-wide grep shows ALLOWED_SSH_CIDRS is consumed by no script and no Terraform variable (it is not TF_VAR_-prefixed), and lab-provision.sh overwrites config/lab-config.env wholesale at lines 332-336 with only LAB_REGION/LAB_ENGINEER_ID/LAB_BUCKET, deleting any customization.
- **Fix:** Make lab-provision.sh accept --ssh-cidr (default to the caller's IP via `curl -s ifconfig.me`/32) and pass `-var='allowed_ssh_cidrs=[...]'`; remove the dead ALLOWED_SSH_CIDRS export from the example file or wire it through as TF_VAR_allowed_ssh_cidrs; stop clobbering lab-config.env.
- **Verifier note:** Verified end-to-end. variables.tf:32-36 defaults allowed_ssh_cidrs to ["0.0.0.0/0"], which flows via student-lab/main.tf:43 into the bastion SG port-22 ingress (networking/main.tf ~208-214). The bastion gets a public IP and is actively used as an SSH jump host by lab-connect.sh, so the exposure is real. lab-provision.sh's plan/apply pass only engineer_id/region/student_bucket/node_count/enable_* — never allowed_ssh_cidrs — and no tfvars or TF_VAR_ exports exist anywhere. ALLOWED_SSH_CIDRS in lab-config.env.example:7-8 (and README.md:192) is consumed by nothing (Terraform only reads TF_VAR_-prefixed env vars), and lab-provision.sh:332-336 confirms the wholesale overwrite of lab-config.env with only LAB_REGION/LAB_ENGINEER_ID/LAB_BUCKET. Mitigation: SSH is key-auth only, so 'major' (not critical) is the right severity. Not covered by the prior audit (its known infra items are k0sctl.yaml.tpl orphan and ttl_hours non-enforcement; security items there concern IAM keys/Grafana/kubeconfig, not the bastion SG). Recommendation is a genuine improvement: a course that documents 'IMPORTANT: Restrict to your IP ranges' while the knob is wired to nothing teaches a false security control.

### F31. k0rdent UI admin password is baked into EC2 user-data and a world-readable file, with no IMDSv2 enforcement on any instance that also carries a broad CAPA/CCM instance role
- **Dimension:** infra | **Verification:** high
- **File:** `lab-infrastructure/terraform/modules/k0rdent-mgmt/main.tf` L56-67, 216-251; templates/mgmt-cloud-init.yaml 45-56; modules/iam/main.tf 184-246
- **Evidence:** main.tf lines 56-67 render the password into user-data: `node_user_data = base64gzip(templatefile(".../mgmt-cloud-init.yaml", { ... ui_password = local.effective_ui_password ... }))`. mgmt-cloud-init.yaml writes it to disk world-readable: `path: /opt/k0rdent-lab/config/lab-info.env` containing `K0RDENT_UI_PASSWORD=${ui_password}` with `permissions: '0644'`. `grep -rn "metadata_options|http_tokens" lab-infrastructure/terraform/` returns nothing — every aws_instance (mgmt, bastion, gpu, metal3, kubevirt) allows IMDSv1, so any pod/process on the cluster can read the full user-data (including the admin password) via http://169.254.169.254/latest/user-data, and can harvest instance-role credentials. That role (iam/main.tf Sids CAPIProviderAWSEC2/CAPIProviderAWSIAM) grants `ec2:RunInstances`, `ec2:CreateVpc`, `ec2:TerminateInstances`, `iam:PassRole`, `iam:CreateInstanceProfile` with `Resource = "*"` — yet the actual CAPA flow taught in Lab 1.3 uses the static aws-cluster-identity-secret, so these instance-role permissions are unused privilege sitting one IMDS call away from any workload pod.
- **Fix:** Add `metadata_options { http_tokens = "required", http_put_response_hop_limit = 1 }` to all aws_instance resources; deliver the UI password via SSM Parameter Store/Secrets Manager fetched at boot (the role already has SecretsManagerRead scoped to k0rdent-training/<id>/*) instead of user-data; chmod lab-info.env 0600; strip the unused CAPA statements from the instance role.
- **Verifier note:** Verified in-repo. (1) k0rdent-mgmt/main.tf:56-67 renders ui_password into node_user_data; user_data_base64 set on aws_instance.mgmt_node (line 239). (2) mgmt-cloud-init.yaml:45-56 writes /opt/k0rdent-lab/config/lab-info.env with K0RDENT_UI_PASSWORD and permissions '0644' (world-readable). (3) grep for metadata_options/http_tokens across the whole terraform tree returns zero matches (exit 1) - all 5 aws_instance resources default to http_tokens=optional, i.e. IMDSv1 allowed; CCM runs hostNetworking:true so IMDS is reachable. (4) iam/main.tf:184-246 (CAPIProviderAWSEC2/CAPIProviderAWSIAM) grants ec2:RunInstances, ec2:CreateVpc, ec2:TerminateInstances, iam:PassRole, iam:CreateInstanceProfile on Resource="*"; mgmt node carries this profile via student-lab/main.tf:80. (5) Lab 1.3 teaches CAPA auth via AWSClusterStaticIdentity + aws-cluster-identity-secret (static keys), confirming the CAPA instance-role perms are an unused-but-harvestable privilege. The recommended SSM/Secrets Manager path is already partly enabled (role has SecretsManagerRead scoped to k0rdent-training/<id>/* at iam/main.tf:106-112). One minor evidence overstatement: the bastion is listed as carrying the broad role, but student-lab/main.tf only wires the profile to mgmt/gpu/metal3/kubevirt (bastion module line 48 gets none); the finding's title is correctly scoped so this does not affect the core claim. Not covered by the prior week-1 audit (the 'plaintext long-lived IAM keys' item concerns the separate aws-cluster-identity-secret, not the UI password in user-data, IMDSv2, the 0644 file, or instance-role harvesting). Real, accurate, non-duplicate, and the remediations would materially improve a paid enterprise course that teaches AWS infra patterns by example.

### F32. k0rdent UI is served over plaintext HTTP through an internet-facing load balancer, with NodePorts opened to 0.0.0.0/0
- **Dimension:** infra | **Verification:** high
- **File:** `lab-infrastructure/terraform/modules/k0rdent-mgmt/templates/mgmt-cloud-init.yaml` L342-371; main.tf 189-196; lab-provision.sh 155, 362
- **Evidence:** The Gateway has a single cleartext listener: `listeners: - name: http / protocol: HTTP / port: 80` routing to `kcm-k0rdent-ui port 3000` (mgmt-cloud-init.yaml 349-371). The mgmt SG opens the CCM NodePort range to the world: `description = "Envoy Gateway LoadBalancer (dynamic NodePort via CCM)" from_port = 30000 to_port = 32767 ... cidr_blocks = ["0.0.0.0/0"]` (k0rdent-mgmt/main.tf 189-196). lab-provision.sh prints `echo "http://${gw_addr}"` (line 155) and the admin password in the same banner; lab-connect.sh --ui-url likewise prints `URL: http://${GW_ADDR}` (line 315). Every login sends `admin/<password>` basic-auth in cleartext across the public internet for the duration of the 6-week course.
- **Fix:** Terminate TLS at the Gateway (self-signed cert via the cert-manager already installed by the k0rdent chart, listener protocol HTTPS/443), or default to an SSH tunnel (`lab-connect.sh --tunnel`) and make the public NLB opt-in; at minimum restrict the 30000-32767 ingress to the student's IP (same allowed_ssh_cidrs).
- **Verifier note:** All cited evidence verified verbatim: Gateway has a single HTTP:80 listener routing to kcm-k0rdent-ui:3000 (mgmt-cloud-init.yaml 349-371) with no TLS listener; mgmt SG opens 30000-32767 to 0.0.0.0/0 (k0rdent-mgmt/main.tf 189-196); lab-provision.sh prints http URL (155) and admin password in the same banner (360-365); lab-connect.sh --ui-url prints http URL + password (315-317). The internet-facing claim holds: no aws-load-balancer-internal annotation exists, networking/main.tf tags public subnets with kubernetes.io/role/elb=1 (lines 87-88), and the design intent is browser access from the student's machine (the node only has a private IP behind a bastion, so the LB is the access path). Auth is confirmed HTTP basic auth (cloud-init line 298: --set k0rdent-ui.auth.basic.password), meaning credentials cross the public internet in cleartext on every request, not just login. Not covered by any prior-audit item (closest items — plaintext IAM keys posture, Grafana 0.0.0.0 bind, NLB-vs-ELB content contradiction — are distinct). Recommendation is actionable: k0rdent's kcm chart ships cert-manager, Envoy Gateway supports TLS listeners, and lab-connect.sh already has tunnel machinery. One caveat for the SG-restriction option: NodePort ingress restricted to student CIDRs also needs the VPC CIDR for NLB health checks. Severity 'major' is appropriate — the admin credential controls a management cluster holding AWS provisioning credentials.

### F33. --nodes N provisions N independent single-node clusters, not the HA cluster the variable and flag advertise
- **Dimension:** infra | **Verification:** high
- **File:** `lab-infrastructure/terraform/modules/k0rdent-mgmt/main.tf` L216-239; variables.tf (env) 45-49; lab-provision.sh 181
- **Evidence:** Every node in `resource "aws_instance" "mgmt_node" { count = var.node_count ... user_data_base64 = local.node_user_data }` receives the identical cloud-init, and that cloud-init unconditionally bootstraps a standalone cluster on each instance: `sudo k0s install controller --config /etc/k0s/k0s.yaml --enable-worker` (mgmt-cloud-init.yaml line 108) followed by a full k0rdent Enterprise + Envoy Gateway install (scripts 03/04). The k0sctl multi-node config is rendered and parked in S3 (main.tf 295-304) but never executed. Yet variables.tf advertises `"Number of management cluster nodes (1 for lab, 3 for HA)"` and lab-provision.sh exposes `--nodes <n>  Number of k0rdent management nodes (default: 1)`. A student running `--nodes 3` pays for 3x t3.xlarge plus 3 CCM-provisioned NLBs and gets three disjoint clusters, with the scripts only ever talking to node [0].
- **Fix:** Either remove --nodes/node_count (single-node is the only supported topology) or implement real multi-node bootstrap (k0sctl from the bastion, or controller-join tokens) and update the variable description; add a validation block rejecting node_count > 1 until then.
- **Verifier note:** Verified line-by-line. main.tf:216-251 gives every mgmt_node (count = var.node_count) the identical user_data; mgmt-cloud-init.yaml:108 unconditionally runs 'k0s install controller --config /etc/k0s/k0s.yaml --enable-worker' with per-node api.address and local etcd, then installs CCM + k0rdent Enterprise + Envoy Gateway per node — so N nodes = N disjoint single-node clusters, each provisioning its own CCM LoadBalancer. The k0sctl config (main.tf:295-304) is only rendered to S3/outputs; grep confirms no script ever runs k0sctl apply. environments/student-lab/variables.tf:45-49 advertises '1 for lab, 3 for HA' and lab-provision.sh:181 exposes --nodes; only node[0] is ever used (module outputs.tf:15/69/75/81, lab-provision.sh:317). Aggravating details the finding missed: module variables.tf defaults node_count=3 (validation allows up to 5), and all nodes share the same kubernetes.io/cluster/<name>=owned SG tag, so the N independent CCMs would contend over the same tagged AWS resources. Minor quibble only: LBs may be Classic ELBs rather than NLBs absent the nlb annotation, though main.tf's own SG comment says NLB. Not a duplicate: the prior audit's orphaned-k0sctl.yaml.tpl item covers only the CNI-mismatch/orphaned-template aspect, not the broken/costly --nodes knob; the audit explicitly allows verify+expand on that item. Recommendation (reject node_count>1 via validation or implement real multi-node join) is a cheap, legitimate fix for a paid course.

### F34. Re-running lab-provision.sh with one feature flag silently destroys the other optional environments — the usage text documents this destructive pattern as 'Add'
- **Dimension:** infra | **Verification:** high
- **File:** `lab-infrastructure/scripts/lab-provision.sh` L194-197, 306-313, 370-375
- **Evidence:** Defaults are reset on every invocation (lines 373-375: `ENABLE_GPU="false" ENABLE_METAL3="false" ENABLE_KUBEVIRT="false"`) and always passed to apply (lines 311-313: `-var="enable_gpu_lab=${ENABLE_GPU}" -var="enable_metal3=${ENABLE_METAL3}" -var="enable_kubevirt=${ENABLE_KUBEVIRT}"`), which drives `count = var.enable_gpu_lab ? 1 : 0` in student-lab/main.tf (lines 104-158). The usage examples present these as additive: `$0 john-doe --gpu  # Add GPU lab` then `$0 john-doe --metal3 --kubevirt  # Add Metal3 + KubeVirt` (lines 195-197). Running the second documented example after the first sets enable_gpu_lab=false and Terraform destroys the GPU environment (a g5.12xlarge and its 200GB+500GB volumes) — with --auto-approve, without any prompt — losing whatever state the student had built on it.
- **Fix:** Read previously-applied flags from the saved config or `terraform output`/state and merge them with the new CLI flags (flags only ever turn features on; add an explicit --disable-gpu etc. for teardown), or at minimum make the usage text warn that every run must restate ALL desired flags.
- **Verifier note:** Verified end-to-end: lab-provision.sh resets ENABLE_GPU/ENABLE_METAL3/ENABLE_KUBEVIRT to "false" on every run (lines 373-375), always passes them explicitly to terraform apply (lines 311-313), and persists nothing about them in lab-config.env (lines 332-336). student-lab/main.tf gates each optional environment with count = var.enable_X ? 1 : 0 (lines 105/124/143), so re-running with a different flag set flips count 1->0 and Terraform destroys the previously-enabled environment (shared GPU = g5.12xlarge with 200GB root + 500GB model EBS, both defaults confirmed in modules/gpu-lab). Usage examples at lines 195-197 label the flags "Add GPU lab" / "Add Metal3 + KubeVirt", so running the second documented example after the first destroys the GPU env — silently with --auto-approve (the documented Quick Start pattern), or buried in a plan prompt without it. Not covered by any prior-audit item (C1 and the infra items concern lab-destroy.sh and ttl/k0sctl, not provision re-runs). Mitigating context only: Week 5 labs README says --gpu is not needed for the curriculum's GPU path, so curriculum-mandated flows mostly avoid the footgun; the hazard lives in the script's own documented usage. Minor nit: usage examples are at lines 195-197, not 194-197. Recommendation (persist/merge flags from saved config, or fix the "Add" wording) is proportionate and would materially improve the course.

### F35. lab-destroy.sh exits 0 with 'No state found' when terraform init fails or the region/bucket mis-resolves, leaving the full environment (EC2, NAT, EIP) billing silently
- **Dimension:** infra | **Verification:** high
- **File:** `lab-infrastructure/scripts/lab-destroy.sh` L161-170, 197-200
- **Evidence:** Lines 162-165: `terraform init -reconfigure -backend-config=... &>/dev/null || true` — init failures (no credentials, wrong region, missing bucket, S3 5xx) are swallowed. Lines 167-170: `if ! terraform state list &>/dev/null 2>&1; then log_warn "No state found for $engineer_id."; return 0; fi` — the script then exits success without destroying anything. The bucket name embeds the region (`k0rdent-lab-${engineer_id}-${account_id}-${REGION}`, line 51) and REGION is resolved from the shared single-slot config/lab-config.env (lines 198-200), which lab-provision.sh overwrites on every run for any student (lab-provision.sh 332-336). On a shared instructor machine, provisioning student B in eu-west-1 then destroying student A (provisioned in us-east-1) resolves the wrong bucket name, init fails, and the script reports a benign 'No state found' while A's VPC/NAT/t3.xlarge keep running. This is a distinct failure mode from the known C1 (ClusterDeployment orphans).
- **Fix:** Do not suppress init output; verify the bucket actually exists (`aws s3api head-bucket`) and abort with a hard error listing candidate buckets (`aws s3 ls | grep k0rdent-lab-<id>`) instead of returning 0; distinguish 'state object absent' from 'backend unreachable'.
- **Verifier note:** Verified line-by-line. lab-destroy.sh:162-165 suppresses terraform init failures (&>/dev/null || true); :167-170 treats any 'terraform state list' failure (including 'Initialization required' after a failed init) as 'No state found' and returns 0 without destroying. Bucket name (line 51) embeds REGION, and REGION resolves from single-slot config/lab-config.env (LAB_REGION) which lab-provision.sh:332-336 overwrites on every run — so destroying student A after provisioning student B in another region (or from a fresh clone with a different default region) resolves a nonexistent bucket and silently exits success while EC2/NAT/EIP keep billing. The script structurally cannot distinguish 'already destroyed' from 'backend unreachable/wrong bucket'. Minor over-claim only: the no-credentials sub-case would abort earlier at aws sts get-caller-identity (line 50) under set -e. Not a duplicate of C1 (which covers ClusterDeployment orphans/TTL/LB-cleanup, i.e., resources never attempted) — this is silent skip of the terraform-managed management infra itself. Recommendation (head-bucket precheck, unsuppressed init, distinguish state-absent vs backend-error) is proportionate and worthwhile for a course whose documented teardown is --auto-approve batch destroy.

### F36. Labs 1.6/1.7 expected outputs reference a phantom second cluster and phantom services — the printed self-checks can never match what learners see
- **Dimension:** pedagogy | **Verification:** high
- **File:** `curriculum/week-1-foundations/labs/lab-1.7-multicluster-services.md` L312-324, 418, 462-464, 489-490 (and lab-1.6-deploy-kof.md:457)
- **Evidence:** Week 1 provisions exactly one managed cluster (managed-cluster-01, Lab 1.5), yet Lab 1.7 verification text asserts: 'managed-cluster-02 should show: environment=training,...', 'both managed-cluster-01 and managed-cluster-02', 'baseline-services → 2/2 clusters', and '# managed-cluster-02 should show SERVICES 2/2 (cert-manager + kyverno only)'. Lab 1.6 Step 4 asserts '# Should show: SERVICES 4/4 (nginx + cert-manager + kof-operators + kof-collectors)' — no nginx service exists at that point (ingress-nginx arrives in Lab 1.7). Lab 1.7 then expects 'SERVICES 3/3 (cert-manager + kyverno + ingress-nginx)', ignoring the 3 KOF services deployed in Lab 1.6. The lab's core pedagogical payoff — selective vs fleet-wide targeting — is unobservable with one cluster, since both selectors match the same single cluster.
- **Fix:** Either add an explicit (optional) second-cluster provisioning step at the start of Lab 1.7, or rewrite all expected outputs for the actual single-cluster state (1/1 matches, correct SERVICES counts including KOF services carried over from Lab 1.6) and add a note explaining what would differ with a second cluster. Recompute every SERVICES N/N string against the actual cumulative lab state.
- **Verifier note:** Verified line-by-line. Week 1 creates only managed-cluster-01 (lab-1.5 L180-211, no serviceSpec); 'managed-cluster-02' appears nowhere in Week 1 except Lab 1.7's expected outputs/prose (L312-324, 418, 449, 462-464, 490). Lab 1.6 L457's 'SERVICES 4/4 (nginx + ...)' contradicts the lab's own list of 3 kof-child services, and no nginx service exists (Lab 1.5's nginx-test is a deleted kubectl test deployment; ingress-nginx arrives in Lab 1.7). Lab 1.7 L489's 'SERVICES 3/3' ignores the KOF services deployed on the Core recommended path of prerequisite Lab 1.6 (Parts 1-4 incl. child deployment, no cleanup). The selective-vs-fleet payoff is genuinely unobservable with one cluster (both selectors match it). Not in the prior audit: C2 covers a different Lab 1.7 defect (training-baseline-services name break L575/743/746 + merge-patch); audit doc has zero mentions of cluster-02 or SERVICES-count mismatches. Caveat: nothing fails at execution (the cluster-02 label command at L312 is commented out) — the defect is that printed self-checks can never match observed output, which still warrants 'major' for a paid capstone lab. Recommendation is sound; the single-cluster-rewrite option is the cheaper fix.

### F37. Lab 1.6 tells students their management cluster lacks AWS CCM, directly contradicting Theory 1.2 and Lab 1.1 where CCM is installed and provisioning the UI's NLB
- **Dimension:** pedagogy | **Verification:** high
- **File:** `curriculum/week-1-foundations/labs/lab-1.6-deploy-kof.md` L633, 649, 668
- **Evidence:** Lab 1.6: '#    Note: k0s doesn't include CCM by default' and 'The management cluster uses k0s which doesn't include AWS Cloud Controller Manager by default.' (used to justify why the LoadBalancer networking option is impossible). Theory 1.2 (L435) states the opposite about the same environment: 'In the training lab, CCM runs as a DaemonSet in `kube-system` and uses the **EC2 instance profile**...', and Lab 1.1 (L346) relies on it: 'AWS Cloud Controller Manager automatically provisions a Network Load Balancer for the Gateway's Service.'
- **Fix:** Fix Lab 1.6's Option A and 'Training Environment Limitation' to acknowledge CCM IS running on the management cluster — which means the `kubectl patch svc ... LoadBalancer` exercise would actually work and could become the week's only hands-on CCM payoff (exposing vminsert via NLB and watching child collectors connect). As written, the contradiction both misinforms learners about their own environment and removes a viable, high-value exercise.
- **Verifier note:** Verified. All three quotes exist at the cited lines. Lab 1.6 (L633/649/668) claims the management cluster lacks AWS CCM and that LoadBalancer services would stay <pending>; Theory 1.2 (L433-448, L688) and Lab 1.1 (L217, L346) state CCM runs on that same cluster — and the ground truth sides with them: lab-infrastructure/terraform/modules/k0rdent-mgmt/templates/mgmt-cloud-init.yaml (L242-267) helm-installs aws-cloud-controller-manager into kube-system and sets the node providerID, and the UI's Gateway LB is provisioned by it. Lab 1.6's Option A targets KOF services (vminsert-cluster etc., ns kof) deployed on the management cluster per its own 'Management to Management' mode, so patching to LoadBalancer would actually provision an LB. Not a duplicate: prior audit's 'CCM never exercised hands-on' and NLB-vs-CLB items do not cover Lab 1.6's false no-CCM claim. One qualification on the recommendation: CCM defaults to a Classic ELB without the aws-load-balancer-type:nlb annotation (overlaps the known NLB/CLB item), and end-to-end child-collector connectivity via the LB also depends on security-group rules and vmauth, so the proposed exercise needs those details handled — but the factual fix is clearly warranted.

### F38. Lab 1.7 declares 'Week 1 Complete' and routes learners to Week 2, orphaning Lab 1.8 and the quiz from the navigation flow
- **Dimension:** pedagogy | **Verification:** high
- **File:** `curriculum/week-1-foundations/labs/lab-1.7-multicluster-services.md` L73, 843-859
- **Evidence:** Lab 1.7 prerequisites: 'This is the final lab of Week 1 -- you can run cleanup after completing it.' Its closing section '## Week 1 Complete!' enumerates only Labs 1.1-1.7 ('7. **Lab 1.7**: Deployed services across clusters...') and '## Next Week' links to '../../week-2-bmaas/README.md'. Lab 1.8 (Upgrade, on the README schedule with the quiz) is never linked from Lab 1.7; only Lab 1.8 itself points to the quiz. The 'run cleanup after completing it' permission also conflicts with Lab 1.8 Part 3, which needs the managed cluster ('(Optional) Managed cluster from Lab 1.5 still running').
- **Fix:** Change Lab 1.7's closer to 'Next Lab: Lab 1.8 — Upgrade k0rdent Enterprise', move the 'Week 1 Complete' recap and Week 2 link into Lab 1.8 (which already ends with the quiz link), and replace 'you can run cleanup after completing it' with 'keep the managed cluster running if you plan to do Lab 1.8 Part 3'. Self-paced learners currently skip the upgrade lab and the assessment entirely.
- **Verifier note:** Verified verbatim: lab-1.7 L73 says "This is the final lab of Week 1 -- you can run cleanup after completing it" and L843-859 close with "Week 1 Complete!" (recap of 1.1-1.7 only) + "Next Week" -> week-2-bmaas/README.md, with zero references to Lab 1.8 or the quiz. The README schedules Lab 1.8 and the quiz AFTER Lab 1.7 (Day 5), and Labs 1.1-1.6 all end with "## Next Lab" sections, so Lab 1.7 breaks the established sequential navigation chain; only lab-1.8 L640 links week-1-quiz.md. The cleanup conflict is actually worse than the finding states: Lab 1.8's prerequisites require the management cluster running v1.2.2 (the upgrade target), so running cleanup after 1.7 blocks all of Lab 1.8, not just Part 3 (which needs the optional managed cluster). Not in the prior audit: C1/C2/C4 and the README-objectives item touch Lab 1.7 cleanup mechanics, the capstone resource-name break, Lab 1.8 etcd commands, and missing upgrade objectives, but none document the navigation orphaning or the L73 contradiction. Recommendation is minimal and correct for a paid course; major severity is justified since self-paced learners would skip the upgrade lab and the assessment.

### F39. README cost guidance ('destroy when not in use', $15-25 @ 8h/day) contradicts the labs' actual instruction to never destroy and leave everything running 24/7
- **Dimension:** pedagogy | **Verification:** high
- **File:** `curriculum/week-1-foundations/README.md` L121 (vs labs/lab-1.1-provision-k0rdent.md:523 and labs/lab-1.8-upgrade-k0rdent.md:634)
- **Evidence:** README: 'Running 8 hours/day for 5 days: **~$15-25 total**. Remember to destroy environments when not in use -- see cleanup instructions at the end of each lab.' Lab 1.1 Clean Up: '**Important:** Only destroy if you're done with **all labs in the curriculum**... If you need to stop for the day, leave the management cluster running.' Lab 1.8: '**Do NOT run `lab-destroy.sh` after completing Week 1.**' Following the labs (24/7 for the week) costs roughly $0.24-0.39/hr × 168h ≈ $40-65 for Week 1 alone — and there is no stop/start alternative offered (e.g., EC2 stop instructions).
- **Fix:** Reconcile the two postures: either correct the README estimate to the leave-it-running reality (per-week and per-curriculum totals), or document a supported pause procedure (stop/start the management EC2 instance, NAT/NLB cost caveats). A paying trainee discovering a 2-3x cost overrun versus the syllabus estimate is a trust failure.
- **Verifier note:** Verified verbatim: README.md:121 estimates ~$15-25 assuming 8h/day and says 'destroy environments when not in use -- see cleanup instructions at the end of each lab', while those very cleanup instructions say the opposite — lab-1.1 (~L523) 'Only destroy if you're done with all labs in the curriculum... leave the management cluster running', lab-1.8 (L634-636) 'Do NOT run lab-destroy.sh' with pause guidance of 'simply disconnect'. No stop/start or pause procedure exists anywhere (lab-infrastructure/scripts has only provision/connect/destroy/status/refresh-creds). Math holds: mgmt stack alone (~$0.238/hr per README's own table) is ~$40 for a 24/7 week, $65 with the managed cluster; the curriculum keeps it running through Week 5 (~$200+ total mgmt cost). Lab 1.5 does offer managed-cluster cleanup, but Labs 1.6/1.7 defer it and the dominant mgmt-cluster cost is unavoidable, so this does not rescue the estimate. Not covered by prior audit: C1/TTL items concern destroy-script orphans and unenforced TTL, not the README estimate-vs-posture contradiction or the missing pause procedure. Major severity is fair for paid training; recommendation is actionable.

### F40. Quiz Q19 demands 'the four AWS tags that CCM requires' but Theory 1.2 teaches only three — the fourth tag exists only in the answer key
- **Dimension:** pedagogy | **Verification:** high
- **File:** `curriculum/week-1-foundations/week-1-quiz.md` L115, 164 (vs theory/1.2-k0rdent-components.md:424-428)
- **Evidence:** Q19: 'Explain the four AWS tags that CCM requires and why a missing `providerID`...'. The answer key's tag #4 is '`kubernetes.io/cluster/<cluster-name>=owned` on subnets so CCM only uses subnets belonging to its cluster.' Theory 1.2's 'How CCM Finds Resources' table teaches exactly three tags (EC2 instance cluster tag, `kubernetes.io/role/elb=1` public subnets, `kubernetes.io/role/internal-elb=1` private subnets); its troubleshooting tip lists four CHECKPOINTS but they are 'CCM pod running, instance tag, subnet elb tag, providerID' — not four tags. No theory unit or lab ever presents four tags.
- **Fix:** Add the subnet cluster-ownership tag as a fourth row in Theory 1.2's CCM table (and ideally have learners inspect the actual tags with `aws ec2 describe-tags` in Lab 1.1/1.2), or reword Q19 to ask for the tags/checkpoints as actually taught. As written, a diligent student cannot reconstruct the expected answer from the course material.
- **Verifier note:** Verified at week-1-quiz.md:115/164 and theory/1.2-k0rdent-components.md:423-448. Theory teaches exactly 3 CCM tags; the quiz demands 4, and the answer key's 4th tag (subnet kubernetes.io/cluster/<name>=owned) appears nowhere else in the curriculum (grep confirms only instance-filter uses in Lab 1.5). The theory's 'four points' troubleshooting tip lists checkpoints, not tags, and omits the answer key's tag #4 — so even a diligent student cannot reconstruct the graded answer. The 4th tag is technically defensible per cloud-provider-aws subnet discovery (cluster tag preferred, untagged fallback), so the right fix is the finding's recommendation: add the subnet cluster tag to the theory table or reword Q19. Not a duplicate: prior-audit Q19 item is the NLB-vs-Classic-ELB contradiction, and the CCM/providerID item is about missing hands-on practice, not this taught-vs-quizzed mismatch.

### F41. Lab 1.4 RBAC exercise is incomplete and unverified: 2 of 3 service accounts never bound, ClusterRoles never bound to anyone, no access check — while Quiz Q18 assesses an isolation mechanism every lab explicitly disables
- **Dimension:** pedagogy | **Verification:** high
- **File:** `curriculum/week-1-foundations/labs/lab-1.4-production-configuration.md` L129-192
- **Evidence:** The lab creates `k0rdent-cluster-admin` and `k0rdent-cluster-viewer` ClusterRoles and three SAs ('kubectl create serviceaccount platform-admin... ml-admin... ml-viewer') but creates exactly one binding ('kubectl create rolebinding platform-admin-binding'); ml-admin/ml-viewer and both ClusterRoles are left dangling, and there is no `kubectl auth can-i --as=system:serviceaccount:...` or impersonation test anywhere, so learners get zero feedback that their RBAC works. Meanwhile Quiz Q18 asks learners to 'Include the role of `allowedNamespaces` on provider identity CRDs' — but Lab 1.3 (L251) and Lab 1.5 (L350-351) both set/patch `allowedNamespaces: {}` (allow-all) and never exercise a restriction.
- **Fix:** Complete the exercise: bind ml-admin/ml-viewer, then add a verification block using `kubectl auth can-i create clusterdeployments -n team-ml --as=system:serviceaccount:team-ml:ml-viewer` (expect no) and the admin equivalent (expect yes). Add a short optional exercise that sets `allowedNamespaces.list` and observes the 'Namespace is not permitted' failure, so Q18's assessed skill is actually practiced once.
- **Verifier note:** Verified against curriculum/week-1-foundations/labs/lab-1.4-production-configuration.md: 2 ClusterRoles + 3 SAs created but only one RoleBinding (platform-admin-binding, L189-192); ml-admin/ml-viewer and both ClusterRoles are never bound, and grep confirms no `auth can-i`/`--as=` verification anywhere in the lab. The readiness script (L576-577) only checks role existence while the checklist (L614) claims 'roles and bindings' configured. Quiz Q18 (week-1-quiz.md L113) does assess allowedNamespaces, while Lab 1.3 L251 and Lab 1.5 L217-220/L348-351 set/patch `allowedNamespaces: {}` (allow-all) and the Azure path uses an empty selector — no lab exercises a restriction. RBAC mechanics claim is technically correct (unbound roles grant nothing). Not covered by prior audit (which addresses production-readiness.sh's audit/netpol/encryption gaps and the missing RBAC theory unit, not the unbound-SA/no-verification exercise gap). Caveat: theory/1.3 covers allowedNamespaces conceptually, so Q18 is answerable from theory; severity is pedagogical (incomplete, unverifiable exercise) rather than technical breakage, but 'major' is defensible for a hands-on training product.

### F42. Lab 1.5 prerequisites and Part 1 require the SSH key pair that Lab 1.3 declared optional and explicitly 'not required' for Lab 1.5
- **Dimension:** pedagogy | **Verification:** high
- **File:** `curriculum/week-1-foundations/labs/lab-1.5-provision-managed-cluster.md` L63, 132-150 (vs labs/lab-1.3-configure-aws-provider.md:372, 380-383)
- **Evidence:** Lab 1.5 Prerequisites: '- SSH key pair created for cluster nodes (Lab 1.3)' and Part 1 includes a 'Verify SSH Key Pair' section whose first command is 'aws ec2 describe-key-pairs --key-names k0rdent-clusters --region $AWS_REGION' (errors with InvalidKeyPair.NotFound if skipped). Lab 1.3 Part 6 is titled '(~5 min, Optional)' and states under 'When you can skip it': 'The training labs in this course (Lab 1.5 does not require it)'. Lab 1.5's own ClusterDeployment then comments sshKeyName out ('# sshKeyName: k0rdent-clusters  # Optional'). Lab 1.3's Summary also claims 'Created SSH key pairs for managed cluster node access' as if mandatory.
- **Fix:** Make the three locations consistent: drop the SSH key from Lab 1.5's prerequisites, retitle Part 1's section '(Optional) Verify SSH Key Pair — only if you completed Lab 1.3 Part 6', and note the expected error for students who skipped it. Students who correctly skipped an optional step should not hit a failing verification command and a prerequisites list telling them they're behind.
- **Verifier note:** Verified all four locations: Lab 1.5 line 63 lists the SSH key as an unqualified prerequisite and lines 132-150 verify it with describe-key-pairs (fails InvalidKeyPair.NotFound if absent); Lab 1.3 Part 6 (line 372) is explicitly Optional and says 'Lab 1.5 does not require it'; Lab 1.5 line 205 comments sshKeyName out; Lab 1.3 Summary line 518 claims the key was created unconditionally (its checklist at line 486 correctly says Optional). sshKeyName is genuinely optional in CAPA, so Lab 1.3 is right and Lab 1.5's prereq/verify framing contradicts it. Minor mitigation the finding omits: the verify block includes commented 'If not found, create it' recovery commands, so students aren't stranded — severity is arguably minor/major borderline rather than clearly major. Not a duplicate: the prior audit's related item is the hardcoded us-east-1 region in Lab 1.3's SSH step (affects students who DID create the key), a different defect from this optional-vs-required contradiction. Recommendation (3 small text edits for consistency) is a real improvement, not pedantry.

### F43. Day 5 packs 7 hours — the three most failure-prone labs (KOF, MCS, upgrade) plus the graded assessment — versus 4.5-5.5h on other days
- **Dimension:** pedagogy | **Verification:** high
- **File:** `curriculum/week-1-foundations/README.md` L20-33
- **Evidence:** Schedule table: Day 5 = 'Lab 1.6: Deploy KOF... 3h' + 'Lab 1.7: Multi-Cluster Services 2h' + 'Lab 1.8: Upgrade k0rdent Enterprise 1.5h' + 'Week 1 Assessment 0.5h' = 7h, while Day 1 = 5h, Day 2 = 4.5h, Day 3 = 4.5h, Day 4 = 5.5h. Day 5 stacks the documented hardest lab (1.6, 25-30 pods, four sequential Helm installs with 10m timeouts), the cross-cluster lab whose waits compound any Lab 1.5/1.6 re-provisioning, a management-plane upgrade with 30 min of rollout waiting, and the 80%-pass quiz — with zero buffer between the upgrade and the assessment.
- **Fix:** Rebalance for cohort delivery: move Lab 1.6 to Day 4 (Lab 1.4 has no provisioning waits to fill), move the quiz to a Day 6 morning or make Lab 1.8 Parts 3-4 read-through/demo for instructor-led runs. Any single failure on Day 5 (Helm timeout, cluster re-provision) currently cascades into the graded assessment window — this is where a cohort falls apart.
- **Verifier note:** Verified: README schedule table (curriculum/week-1-foundations/README.md L20-33) gives Day 5 = 3+2+1.5+0.5 = 7h vs 5/4.5/4.5/5.5h on Days 1-4, with the 80%-pass quiz placed directly after the upgrade lab with no buffer. Supporting lab evidence holds: Lab 1.6 has four sequential helm --wait installs (three with explicit --timeout 10m; kof-operators uses the 5m default — minor overstatement) and a documented ~25-30 pod stack (L306); Lab 1.8 documents ~30 min of management-plane rollout waiting (Part 2 header) within a 1.5h/~45min-wait budget. Not a duplicate: the prior audit covers per-lab time-budget padding and the Lab 1.6 difficulty cliff but never day-level load distribution or assessment placement — and its own finding that 1.6-Full is under-budgeted while Lab 1.3 is ~4x padded makes the Day 5 imbalance worse in active-time terms. Caveat: 'cohort falls apart' assumes instructor-led delivery, which the README never specifies; severity could arguably be downgraded for purely self-paced use, but the rebalancing recommendation is concrete and improves the course either way.

### F44. Lab 1.8 Part 3 (upgrade a managed cluster) is unachievable on the 1.2.2→1.2.3 patch path the lab itself uses — instructions dead-end at placeholders while the objectives and summary still claim the skill
- **Dimension:** pedagogy | **Verification:** high
- **File:** `curriculum/week-1-foundations/labs/lab-1.8-upgrade-k0rdent.md` L49, 341, 384
- **Evidence:** Objectives promise 'Upgrade a managed cluster's Kubernetes version via ClusterTemplateChain'. Part 3 Step 1's note concedes: '**Patch releases (e.g., 1.2.2 → 1.2.3)** may not ship new templates... If you only see one template version, you need to upgrade the management plane to a minor release first.' — but the lab's Part 2 upgrade IS 1.2.2→1.2.3, so learners reach Step 3's 'NEW_TEMPLATE="<new-template-name>"  # e.g., aws-standalone-cp-1-0-26' with no valid value to substitute and no instructed fallback. The Summary still hedges to 'Understood managed cluster upgrades' while the Objectives say 'Perform'.
- **Fix:** Restructure Part 3 honestly: gate it with an explicit check ('if only one aws-standalone template exists, this part is a read-through — that is the expected outcome on a patch release'), provide a simulated alternative that IS executable (e.g., create a ClusterTemplateChain and attempt a disallowed template patch to watch the webhook reject it — which also exercises the Q9-assessed concept), and align the lab objectives with what's actually performable.
- **Verifier note:** Verified verbatim at lines 49/56/215/341/384/626 of curriculum/week-1-foundations/labs/lab-1.8-upgrade-k0rdent.md. Empirically confirmed: release.yaml from get.mirantis.com for Enterprise 1.2.2 and 1.2.3 pin identical provider templates, and Mirantis docs for both versions ship the same aws-standalone-cp-1-0-20 — the exact template Lab 1.5 deploys with (line 178). So on the lab's own 1.2.2->1.2.3 path there is guaranteed to be no upgrade target; the example value aws-standalone-cp-1-0-26 exists in neither release, making Part 3 Steps 3-4 unexecutable and their expected output unobservable. Not a duplicate: prior audit covers Lab 1.8 only for C4 (k0s etcd), time budgets, cleanup, and README objectives, and explicitly validated only the Part 2 management-plane flow as correct. Recommendation is sound: Quiz Q9 does assess ClusterTemplateChain availableUpgrades, so the proposed executable webhook-rejection alternative exercises an assessed concept. Severity major is appropriate (objective overclaim + dead-end, but non-destructive and partially flagged by the lab's own note).

### F45. Cheat-sheet Grafana access block is unusable: wrong secret key, no username step, no tunnel context
- **Dimension:** resources | **Verification:** high
- **File:** `curriculum/week-1-foundations/resources/cheat-sheets.md` L117-123
- **Evidence:** Cheat sheet L119: `kubectl get secret -n kof grafana-admin-credentials -o jsonpath='{.data.admin-password}' | base64 -d && echo` — the key `admin-password` does not exist in the grafana-operator secret. Lab 1.6 (labs/lab-1.6-deploy-kof.md L722-727) uses the real keys: `-o jsonpath='{.data.GF_SECURITY_ADMIN_USER}'` and `-o jsonpath='{.data.GF_SECURITY_ADMIN_PASSWORD}'`. The cheat-sheet command silently prints an empty string. The block also never retrieves the username (Lab 1.6 fetches GF_SECURITY_ADMIN_USER because it is not guaranteed to be 'admin'), and L122-123 say `kubectl port-forward svc/grafana-vm-service -n kof 3000:3000` then 'Open http://localhost:3000' — but in this lab environment the command runs on the management node behind a bastion; Lab 1.6 L752-754 requires an SSH tunnel (`-L 3000:localhost:3000` plus `--address 0.0.0.0`) for the browser to reach it.
- **Fix:** Replace the jsonpath key with GF_SECURITY_ADMIN_PASSWORD, add a line fetching GF_SECURITY_ADMIN_USER, and add the SSH-tunnel one-liner from Lab 1.6 (or a pointer to it) so 'Open http://localhost:3000' actually works from the learner's laptop.
- **Verifier note:** Confirmed: cheat-sheets.md L119 uses jsonpath key 'admin-password', the only occurrence in the repo; grafana-operator's grafana-admin-credentials secret only has GF_SECURITY_ADMIN_USER/GF_SECURITY_ADMIN_PASSWORD (as Lab 1.6 L719-727 correctly uses), so the command silently prints empty output. The block also omits username retrieval and the bastion SSH tunnel that Lab 1.6 L737-756 shows is required for 'Open http://localhost:3000' to work from the learner's machine. Not covered by any prior-audit item (the '0.0.0.0' item is a different, security-side concern). Minor caveat: tunnel omission alone is arguable cheat-sheet brevity, but the dead secret key alone justifies major severity.

### F46. Troubleshooting decision tree uses an invented EC2 tag filter (tag:cluster) that always returns empty
- **Dimension:** resources | **Verification:** high
- **File:** `curriculum/week-1-foundations/resources/cheat-sheets.md` L212
- **Evidence:** L212: `└── Check AWS: aws ec2 describe-instances --filters "Name=tag:cluster,Values=<name>"` — CAPA does not tag instances with a `cluster` key. Lab 1.5 (labs/lab-1.5-provision-managed-cluster.md L292 and L589) uses the real tag: `--filters "Name=tag:kubernetes.io/cluster/managed-cluster-01,Values=owned"`. The cheat-sheet filter matches nothing, so a learner triaging a stuck-in-Provisioning cluster concludes no AWS resources were created while EC2 instances may actually be running and billing (compounding the already-documented orphaned-resource cleanup gap).
- **Fix:** Change the filter to `"Name=tag:kubernetes.io/cluster/<name>,Values=owned"` to match Lab 1.5 and actual CAPA tagging.
- **Verifier note:** Verified verbatim at cheat-sheets.md L212. CAPA never applies a bare `cluster` tag key to EC2 instances (real tags: kubernetes.io/cluster/<name>=owned, sigs.k8s.io/cluster-api-provider-aws/cluster/<name>=owned); confirmed the course adds no additionalTags anywhere (repo-wide grep: zero hits; Lab 1.5 ClusterDeployment config sets only clusterLabels, which are K8s labels, not EC2 tags). So the filter always returns empty, producing a false 'no AWS resources' conclusion in the stuck-Provisioning decision tree. Lab 1.5 L293/L590 (finding said L292/L589, off-by-one, immaterial) use the correct filter, so the fix is consistent with existing content. Not covered by any prior-audit item: C1 is about destroy-script cleanup, not this diagnostic command. Recommendation is correct and trivially applied.


## MINOR (19)

### F47. All five lab scripts emit ANSI color escapes unconditionally — no TTY check or NO_COLOR support, so piped logs, CI captures, and braille/screen-reader output are garbled with escape codes
- **Dimension:** access | **Verification:** unverified
- **File:** `lab-infrastructure/scripts/lab-provision.sh` L12-21 (same pattern: lab-connect.sh 11-20, lab-destroy.sh 12-21, lab-status.sh 11-16, lab-refresh-creds.sh 16-20)
- **Evidence:** lab-provision.sh L12-18: "RED='\\033[0;31m'\nGREEN='\\033[0;32m'\n...\nlog_info() { echo -e \"${BLUE}[INFO]${NC} $1\"; }" — colors are always interpolated; there is no [ -t 1 ] test and no NO_COLOR/TERM=dumb handling in any of the five scripts (verified by grep). A student capturing the ~20-minute provision run ('./scripts/lab-provision.sh ... | tee provision.log') gets literal \033[0;34m sequences on every status line. The bracketed text labels ([INFO]/[OK]/[WARN]/[ERROR]) are good practice and already carry the meaning.
- **Fix:** Gate color assignment on terminal detection: if [ -t 1 ] && [ -z "${NO_COLOR:-}" ]; then RED=...; else RED=''; GREEN=''; ...; fi in each script. No other change needed since text labels already exist.

### F48. Provisioning waits of up to ~25 minutes signal progress only as bare unlabeled dots — no elapsed time or state for screen-reader users or anyone returning to the terminal
- **Dimension:** access | **Verification:** unverified
- **File:** `lab-infrastructure/scripts/lab-provision.sh` L47, 68, 96, 159
- **Evidence:** Four wait loops print only 'echo -n "."' between checks: instance-running (L47, 5s interval up to 600s), status checks (L68, 10s), SSH-ready (L96, 5s × 20), and Gateway LB address (L159, 10s × 24, '24 × 10s = 4 min'). A screen reader announces a stream of periods with no phase, progress, or elapsed-time information; if the user steps away, the screen shows only dot rows with the last [INFO] label scrolled off.
- **Fix:** Replace or supplement the dot stream with a periodic labeled line, e.g. every 6th iteration: log_info "still waiting for instance state=running (${elapsed}s elapsed, timeout ${max_wait}s)". Keep the dots if desired but never as the sole signal.

### F49. Lab 1.3 credential-rotation procedure assumes the AWS Console (visual web UI) with no CLI equivalent, breaking the lab's otherwise CLI-first, screen-reader-friendly flow
- **Dimension:** access | **Verification:** unverified
- **File:** `curriculum/week-1-foundations/labs/lab-1.3-configure-aws-provider.md` L447-458, 496
- **Evidence:** L448-449: "# To rotate credentials:\n# 1. Create new AWS access key in AWS Console"; L457: "# 3. Delete old AWS access key from AWS Console"; Troubleshooting L496: "Check if access key is active in AWS console". Steps 1 and 3 of a procedure the Validation Checklist requires ('[ ] Understand credential rotation process', L488) are only described as console actions, while every other operation in the lab is a copy-pasteable command.
- **Fix:** Add the CLI equivalents inline: aws iam create-access-key --user-name <user>, aws iam update-access-key --status Inactive ..., aws iam delete-access-key --access-key-id <old-key>, and aws iam list-access-keys for the troubleshooting check.

### F50. Lab 1.7 Part 7 validation commands give no expected-output examples — learners (especially ESL) cannot self-verify whether cert-manager/ingress/kyverno tests passed
- **Dimension:** access | **Verification:** unverified
- **File:** `curriculum/week-1-foundations/labs/lab-1.7-multicluster-services.md` L634-636, 715-722
- **Evidence:** L634-636: "# Verify certificate was issued\nkubectl get certificate test-cert\nkubectl get secret test-cert-tls" — no expected output shown (e.g., READY True), unlike Lab 1.1 (L279-283 'Expected output:') and Lab 1.5 which print expected-result blocks after verification commands. Same for kyverno: L718-722 "# Test the policy (this pod should trigger a policy event)\nkubectl run test-pod ...\nkubectl get policyreport -A" with no indication of what a passing policyreport looks like, then immediate cleanup at L724-726.
- **Fix:** After each verification command in Part 7, add a short 'Expected output:' block (Certificate READY True, ingress ADDRESS populated, policyreport with PASS/WARN counts) matching the convention already used in Labs 1.1 and 1.5.

### F51. Untranslated idioms and undefined jargon in core theory text create ESL friction: 'single pane of glass', 'mothership' used before definition
- **Dimension:** access | **Verification:** unverified
- **File:** `curriculum/week-1-foundations/theory/1.1-k0rdent-architecture.md` L74 (also theory/1.2 L561)
- **Evidence:** Theory 1.1 L74: "- No single pane of glass for fleet health" — idiom carrying the key concept of the 'Lack of Visibility' problem statement, with no plain-language restatement. Theory 1.2 L561: "KOF deploys collectors to workload clusters that forward metrics to the mothership:" — first use of 'mothership' in the curriculum with no definition; the term is only explained later in Lab 1.6 (kof-mothership chart). Overall idiom density in Week 1 is otherwise low (grep across all files found only these plus one 'gotcha' in Lab 1.7 that is immediately explained).
- **Fix:** Restate idioms in plain language on first use: 'No single unified view (single pane of glass) of fleet health'; in theory 1.2, introduce mothership as 'the central KOF installation on the management cluster, called the mothership (after the kof-mothership Helm chart)'.

### F52. Root README misdescribes Week 1 ('GPU HW', 'BMC', BMaaS weeks 1-2) against both the overview and Week 1's actual content; appendix glossary defines zero of Week 1's core k0rdent terms despite being billed as covering the curriculum
- **Dimension:** crossweek | **Verification:** unverified
- **File:** `README.md` L35, 48-50 (vs overview L25/L62-64/L152, appendix/glossary.md)
- **Evidence:** Root README L48-50 learning-path box for Week 1: "│• k0rdent│ ... │• GPU HW │ ... │• BMC │" and L35: "**BMaaS** | 1-2 |" — but Week 1 README objectives (L10-16) contain no GPU/BMC/bare-metal content, and the overview's versions of the same tables say "**BMaaS** | 2 |" (L25) and Week 1 = "• k0rdent • CAPI • KOF" (L62-64). Separately, overview L152 says "See the [full glossary](../appendix/glossary.md) for definitions of ... terms used throughout this curriculum", yet grep of curriculum/appendix/glossary.md finds no entries for KCM, KSM, KOF, ClusterDeployment, ServiceTemplate, MultiClusterService, or Sveltos — the only k0rdent mention is the DCME entry at L48.
- **Fix:** Sync the root README's learning-path diagram and product/week table with the overview (BMaaS = Week 2; Week 1 = k0rdent/CAPI/KOF), and add glossary entries for the k0rdent-specific vocabulary Week 1 introduces (KCM, KSM, KOF, ClusterDeployment, Credential, ClusterTemplate, ServiceTemplate, MultiClusterService, Release, ManagementBackup).

### F53. Lab 1.6 KOF cluster ConfigMap hardcodes aws_region: eu-west-1 inside a quoted heredoc — wrong for students in any other region, with no substitution possible
- **Dimension:** drilldown | **Verification:** unverified
- **File:** `curriculum/week-1-foundations/labs/lab-1.6-deploy-kof.md` L401-427
- **Evidence:** L402 uses a quoted heredoc ("cat << 'EOF' | kubectl apply -f -") so nothing expands, and L417 hardcodes "  aws_region: eu-west-1" in the kof-cluster-config-managed-cluster-01 ConfigMap. Lab 1.1's region table recommends us-east-1/us-west-2/eu-west-1 and Lab 1.5 told students to set $AWS_REGION to THEIR region; students outside eu-west-1 copy-paste an incorrect region into the child-cluster telemetry config with no placeholder marker telling them to edit it.
- **Fix:** Use an unquoted heredoc with `aws_region: ${AWS_REGION}` (and re-source /opt/k0rdent-lab/config/lab-info.env first), or mark the line with the course's placeholder convention (`<your-region>`).

### F54. Lab 1.7 Part 8 'Update Service Values' patch passes un-nested values, directly contradicting the lab's own wrapper-chart gotcha — values would be silently ignored
- **Dimension:** drilldown | **Verification:** unverified
- **File:** `curriculum/week-1-foundations/labs/lab-1.7-multicluster-services.md` L367-384 vs 746-747
- **Evidence:** The lab's own callout (L367, L377-382) warns: "any values you pass must be **nested under the subchart name** ... ❌ WRONG — silently ignored by wrapper, chart installs with defaults". Yet the Part 8 patch example (L746-747) passes top-level values: "kubectl patch multiclusterservice training-baseline-services -n kcm-system \\\n  --type=merge -p '{\"spec\":{\"serviceSpec\":{\"services\":[{\"template\":\"ingress-nginx-4-11-0\",...,\"values\":\"controller:\\n  replicaCount: 2\\n\"}]}}}'" — `controller:` is not nested under `ingress-nginx:`, so even after fixing the known C2 name/merge problems, the replica change would be silently ignored per the lab's own warning.
- **Fix:** Change the patch's values payload to "ingress-nginx:\n  controller:\n    replicaCount: 2\n" so Part 8 is consistent with the wrapper-chart guidance taught in Part 4.

### F55. Lab 1.3/1.5 use the fixed, account-wide EC2 key-pair name 'k0rdent-clusters' — in a shared cohort account, student #2's import fails with InvalidKeyPair.Duplicate, and anyone who then sets sshKeyName is silently using another student's key
- **Dimension:** floor | **Verification:** unverified
- **File:** `curriculum/week-1-foundations/labs/lab-1.3-configure-aws-provider.md` L388-392 (plus lab-1.5 L144, L205)
- **Evidence:** lab-1.3 L388: 'ssh-keygen -t ed25519 -f /tmp/k0rdent-clusters -N ""' then L391-392: 'aws ec2 import-key-pair \\ --key-name k0rdent-clusters' — a hardcoded name that is unique per account+region, unlike every Terraform resource in the repo which is namespaced by engineer_id (e.g., iam/main.tf L31 '"${local.name_prefix}-lab-instance-${var.region}"'). In a shared account the second student's import errors with InvalidKeyPair.Duplicate; if they shrug and proceed, lab-1.5 L144 'aws ec2 describe-key-pairs --key-names k0rdent-clusters' passes (the other student's key exists) and uncommenting 'sshKeyName: k0rdent-clusters' (L205) launches their nodes with a key whose private half belongs to a classmate — both an access failure and a cross-student access grant.
- **Fix:** Namespace the key name with the engineer id: '--key-name k0rdent-clusters-<your-engineer-id>' in lab-1.3 Part 6 and the matching describe/sshKeyName references in lab-1.5, and add one line: 'If import fails with InvalidKeyPair.Duplicate, another student already used this name — pick a unique one.'

### F56. Lab 1.5's copy-paste region line defaults to eu-west-1 while every other Week 1 example uses us-east-1 — a pasted mismatch provisions the managed cluster in a region the student never inspects, compounding the known orphan-cost gap
- **Dimension:** floor | **Verification:** unverified
- **File:** `curriculum/week-1-foundations/labs/lab-1.5-provision-managed-cluster.md` L139-141 vs lab-1.1 L192 and README quick start
- **Evidence:** lab-1.5 L141: 'export AWS_REGION="eu-west-1"  # <-- adjust to YOUR region', while lab-1.1's worked example (L192) is './lab-provision.sh john-doe --region us-east-1 --auto-approve' and its sample output (L353) shows 'http://xxxxx.elb.us-east-1.amazonaws.com'. The only guard is the inline comment. CAPA happily provisions into eu-west-1 with account-wide credentials: sshKeyName is commented out (L205) so nothing fails fast — the student gets a working-looking ClusterDeployment whose EC2/NAT/VPC live in a region neither their console habit, their cost checks, nor any cleanup verification will look at. This multiplies the already-documented C1 teardown gap (different failure: wrong-region orphan rather than undeleted ClusterDeployment, so even a future 'delete all ClusterDeployments' fix would clean it, but a student who deletes the CD after noticing the wrong region and recreates may still leave the eu-west-1 stack mid-provision).
- **Fix:** Derive instead of asking: 'export AWS_REGION=$(grep LAB_REGION lab-infrastructure/config/lab-config.env | cut -d\" -f2)' or instruct 'export AWS_REGION=<the region you passed to lab-provision.sh>' with a verification line 'aws ec2 describe-instances --filters Name=tag:EngineerID,Values=<id> --region $AWS_REGION' that proves the management VPC is visible in that region before continuing.

### F57. AWS secret keys and session tokens are passed as SSH remote-command arguments in the credential-refresh path — visible in the management node's process table and inconsistent with the course's own secrets-hygiene teaching
- **Dimension:** floor | **Verification:** unverified
- **File:** `lab-infrastructure/scripts/lab-refresh-creds.sh` L148-153 (plus curriculum/week-1-foundations/labs/lab-1.3-configure-aws-provider.md 213-222)
- **Evidence:** lab-refresh-creds.sh L148-153 builds the remote command with locally-expanded secrets: 'ssh ... ubuntu@"${MGMT_IP}" "kubectl delete secret aws-cluster-identity-secret ... kubectl create secret generic aws-cluster-identity-secret -n kcm-system --from-literal=AccessKeyID='${AWS_ACCESS_KEY_ID}' --from-literal=SecretAccessKey='${AWS_SECRET_ACCESS_KEY}' --from-literal=SessionToken='${AWS_SESSION_TOKEN:-}'"' — the full secret access key and session token appear in the remote bash and kubectl argv (readable via ps//proc by any process on the node, and recorded if auditd/process accounting is on). Lab 1.3's manual variant (L213-222) teaches the same pattern verbatim. The course is mid-effort to clean up plaintext-secret muscle memory (TRAP 22 commits), and this channel — secrets in process arguments — is a distinct leak vector from the already-documented 'plaintext keys in shell exports / base64 in etcd' posture.
- **Fix:** Pipe the secret over stdin instead of argv: generate the Secret manifest locally ('kubectl create secret generic ... --dry-run=client -o yaml') and pipe it through 'ssh ... "kubectl apply -f -"', in both the script and the Lab 1.3 snippet; add the one-line teaching note 'never put secrets in command arguments — they are visible in the process table'.

### F58. Cloud-init installs unpinned kubectl/helm/flux/CCM-chart while the pinned flux_version variable is dead, and the provider lockfile is gitignored — environments drift between students and from the k8s 1.32 skew policy
- **Dimension:** infra | **Verification:** unverified
- **File:** `lab-infrastructure/terraform/modules/k0rdent-mgmt/templates/mgmt-cloud-init.yaml` L142-175, 248-256; main.tf 61; .gitignore (repo root) line 24
- **Evidence:** 02-install-tools.sh fetches floating versions: `curl -LO "https://dl.k8s.io/release/$(curl -L -s https://dl.k8s.io/release/stable.txt)/bin/linux/amd64/kubectl"` (line 142, current stable is 2+ minors ahead of the k0s v1.32.4 API server, outside the documented +/-1 kubectl skew), `curl https://raw.githubusercontent.com/helm/helm/main/scripts/get-helm-3 | bash` (147), `curl -s https://fluxcd.io/install.sh | sudo bash` (161), `curl -sS https://webinstall.dev/k9s | bash` (175, third-party curl|bash), and `helm install aws-cloud-controller-manager ...` with no --version (252-256). Meanwhile main.tf line 61 passes `flux_version = var.flux_version` (default v2.4.0) into the template where it is never referenced — confirmed by grep. The bastion also installs floating kubectl (bastion-cloud-init.yaml line 79). git check-ignore shows `.gitignore:24:.terraform.lock.hcl` ignores the provider lockfile, so every student resolves providers fresh.
- **Fix:** Pin kubectl to v1.32.x to match k0s, pin helm/flux/k9s/CCM chart versions via template variables (actually consuming flux_version), drop the webinstall.dev dependency, and commit .terraform.lock.hcl.

### F59. lab-status.sh reads a Terraform output 'ui_url' that does not exist, so the status command can never display the UI URL
- **Dimension:** infra | **Verification:** unverified
- **File:** `lab-infrastructure/scripts/lab-status.sh` L137-141, 187
- **Evidence:** Lines 138 and 187 query `.outputs.ui_url.value // empty` / `ui_url: (.outputs.ui_url.value // null)` from the S3 state, but terraform/environments/student-lab/outputs.tf defines only `ui_access` ("How to retrieve the k0rdent UI URL", lines 48-51) and `ui_password` — there is no ui_url output (the URL is runtime CCM data, as lab-connect.sh's comment at line 279 acknowledges: 'UI URL is now served via Envoy Gateway, not Terraform output'). The status display and its --json output therefore always omit the UI URL, and the JSON consumers get a permanently-null field.
- **Fix:** Either drop the ui_url field from lab-status.sh or populate it the same way lab-connect.sh --ui-url does (SSH + `kubectl get gateway ... jsonpath`), and surface `ui_access` instead.

### F60. lab-refresh-creds.sh passes the AWS secret access key and session token as SSH command-line arguments, exposing them in process listings on both the local machine and the management node
- **Dimension:** infra | **Verification:** unverified
- **File:** `lab-infrastructure/scripts/lab-refresh-creds.sh` L148-153
- **Evidence:** `ssh ... ubuntu@"${MGMT_IP}" "kubectl delete secret aws-cluster-identity-secret -n kcm-system 2>/dev/null; kubectl create secret generic aws-cluster-identity-secret -n kcm-system --from-literal=AccessKeyID='${AWS_ACCESS_KEY_ID}' --from-literal=SecretAccessKey='${AWS_SECRET_ACCESS_KEY}' --from-literal=SessionToken='${AWS_SESSION_TOKEN:-}'"` — the secret key/token are interpolated locally into the ssh argv (visible via `ps`/audit logs on the workstation) and again into the remote bash and kubectl argv on the shared management node. A value containing a single quote would also break the remote command. The script is the canonical Week-1 recovery path referenced by Lab 1.3 (line 220-221 mirrors the same pattern).
- **Fix:** Pipe the secret over stdin instead: `printf '...' | ssh ... 'kubectl create secret generic aws-cluster-identity-secret -n kcm-system --from-file=AccessKeyID=/dev/stdin ...'` or scp a 0600 manifest and `kubectl apply -f - && shred`; never place credentials in argv.

### F61. Alias table contradicts both Lab 1.1 and the actually-provisioned aliases (kgm, kgct, kgcred)
- **Dimension:** resources | **Verification:** unverified
- **File:** `curriculum/week-1-foundations/resources/cheat-sheets.md` L13-20
- **Evidence:** Cheat sheet documents: L17 `kgm` = `kubectl get management`; L19 `kgct` = `kubectl get clustertemplates -n kcm-system`; L20 `kgcred` = `kubectl get credentials -n kcm-system`. The cloud-init that actually provisions them (lab-infrastructure/terraform/modules/k0rdent-mgmt/templates/mgmt-cloud-init.yaml L550-553) defines: `alias kgm='kubectl get management -A'`, `alias kgct='kubectl get clustertemplates -A'`, `alias kgcred='kubectl get credentials -A'`. Lab 1.1's own alias table (labs/lab-1.1-provision-k0rdent.md L543-545) documents the `-A` versions correctly, so the cheat sheet ('from Lab 1.1') contradicts the lab it cites.
- **Fix:** Copy the alias definitions verbatim from mgmt-cloud-init.yaml (the `-A` forms) so the cheat sheet matches what `alias` shows on the management node.

### F62. Cluster-scoped k0rdent CRDs (Management, ProviderTemplate) presented as namespaced in kcm-system
- **Dimension:** resources | **Verification:** unverified
- **File:** `curriculum/week-1-foundations/resources/cheat-sheets.md` L33-35, 42, 171
- **Evidence:** L34-35: `kubectl get management -n kcm-system` / `kubectl describe management kcm -n kcm-system`; L42: `kubectl get providertemplates -n kcm-system`; L171 exports `kubectl get management,credentials,clustertemplates,servicetemplates -n kcm-system`. Per kcm source, Management is `// +kubebuilder:resource:shortName=kcm-mgmt;mgmt,scope=Cluster` and ProviderTemplate is `// +kubebuilder:resource:shortName=providertmpl,scope=Cluster` (api/v1beta1). kubectl silently ignores -n for cluster-scoped kinds so the commands appear to work, but the cheat sheet teaches the wrong resource scope; every lab uses `kubectl get management kcm` with no namespace (e.g., labs/lab-1.8-upgrade-k0rdent.md L101).
- **Fix:** Drop `-n kcm-system` from management and providertemplates commands and add a one-line note that Management, ProviderTemplate, Release, and MultiClusterService are cluster-scoped while ClusterTemplate/ServiceTemplate/Credential live in kcm-system.

### F63. Cheat sheet references local YAML manifest files that no lab ever creates
- **Dimension:** resources | **Verification:** unverified
- **File:** `curriculum/week-1-foundations/resources/cheat-sheets.md` L142-151, 168
- **Evidence:** Credential section: L142 `kubectl apply -f aws-secret.yaml`, L145 `kubectl apply -f aws-identity.yaml`, L148 `kubectl apply -f aws-cluster-identity-resource-template.yaml`, L151 `kubectl apply -f aws-credential.yaml`; Pre-Upgrade section L168 `kubectl apply -f management-backup.yaml`. Lab 1.3 creates all credential objects inline (`kubectl create secret generic aws-cluster-identity-secret ...` at L176 and `cat << 'EOF' | kubectl apply -f -` heredocs at L242/L262/L278), and Lab 1.8 heredocs the ManagementBackup (L128-134). A learner using the cheat sheet has none of these files on disk.
- **Fix:** Either replace the file references with the heredoc pattern the labs use, or note explicitly that the filenames are placeholders for manifests shown inline in Labs 1.3 and 1.8.

### F64. Troubleshooting tree's credential-chain remediation omits the resource-template ConfigMap the labs require
- **Dimension:** resources | **Verification:** unverified
- **File:** `curriculum/week-1-foundations/resources/cheat-sheets.md` L204
- **Evidence:** L204: `└── Missing? → Create credential chain (Secret → Identity → Credential)` — omits the `aws-cluster-identity-resource-template` ConfigMap that Lab 1.3 creates (L262-266) and verifies as part of the chain (L302, L355), and that the cheat sheet's own Credential section lists as step '2b. Create the resource template ConfigMap' (L147-148). A learner repairing a missing credential chain from the tree alone rebuilds an incomplete chain (the ConfigMap drives CCM/CSI credential propagation to managed clusters).
- **Fix:** Update the tree to 'Secret → Identity → resource-template ConfigMap → Credential' to match Lab 1.3 and the cheat sheet's own Credential section.

### F65. KOF and Service Upgrades reference links point at community docs (OSS latest) instead of the Enterprise 1.2.x docs used everywhere else
- **Dimension:** resources | **Verification:** unverified
- **File:** `curriculum/week-1-foundations/resources/reference-links.md` L41-42, 57
- **Evidence:** L41-42: `https://docs.k0rdent.io/latest/admin/kof/kof-architecture/` and `https://docs.k0rdent.io/latest/admin/kof/kof-install/`; L57: `https://docs.k0rdent.io/latest/user/services/service-upgrade/`. All other k0rdent rows in the file use `https://docs.mirantis.com/k0rdent-enterprise/latest/...`. The Enterprise equivalents exist and resolve (verified HTTP 200: docs.mirantis.com/k0rdent-enterprise/latest/admin/kof/kof-architecture/ and .../admin/kof/kof-install/). Community 'latest' tracks the newest OSS k0rdent release, not Enterprise 1.2.2, so KOF install/upgrade procedures can silently drift from what the paid course teaches. (All 32 URLs in the file do resolve — no dead links found; both doc servers return real 404s for bogus paths.)
- **Fix:** Swap the three docs.k0rdent.io links for their docs.mirantis.com/k0rdent-enterprise/latest equivalents, or add a note that community 'latest' may describe a newer k0rdent version than Enterprise 1.2.2.


## Completeness-critic gaps (areas needing a deliberate pass)

### [critical] Program entry contract never examined: prerequisites.md promises a non-existent 'lab provisioning portal' and omits every real Week 1 requirement (AWS account, admin-level IAM, Terraform, AWS CLI, budget)
- **File:** `curriculum/overview/prerequisites.md` L33-42
- **Evidence:** prerequisites.md 'Technical Requirements' lists only: 'Workstation with 16GB+ RAM, Docker or Podman installed, kubectl installed, Helm installed, Git installed' and for Cloud Labs: 'Browser (Chrome/Firefox recommended), SSH client, Access to lab provisioning portal (provided)' (L42). No portal exists anywhere in the repo — Lab 1.1 actually requires 'AWS CLI v2 installed, Terraform >= 1.8.0 installed, Your own copy of this training repository (fork or template)' and a personal AWS account. Week 1 README L125 says only 'AWS Account with appropriate permissions'; the actual permission level is never quantified anywhere, yet terraform/modules/iam/main.tf creates `aws_iam_role` (L30), `aws_iam_role_policy` (L51), and `aws_iam_instance_profile` (L259), meaning the student's own principal needs iam:CreateRole/PutRolePolicy/CreateInstanceProfile/PassRole — effectively administrator. Docker/Podman, which prerequisites.md does require, is used by no Week 1 lab.
- **Fix:** Rewrite the 'Technical Requirements' section to match the actual fork-and-self-provision delivery model: personal AWS account with a stated IAM bar (AdministratorAccess or a published minimal policy covering EC2/VPC/IAM/S3/ELB), AWS CLI v2, Terraform >= 1.8.0, git + GitHub account, supported OS/shell, and an explicit weekly budget figure. Delete the 'lab provisioning portal' line or implement the portal. Add a finder pass over curriculum/overview/ (prerequisites.md and training-program-overview.md were outside every reviewed dimension except duration math).

### [major] Capacity/sizing dimension wholly unexamined: single 16GB management node must host the entire management plane plus the full KOF mothership stack, and one t3.medium (4GB) worker accumulates collectors + cert-manager + kyverno + ingress-nginx by Lab 1.7
- **File:** `curriculum/week-1-foundations/labs/lab-1.5-provision-managed-cluster.md` L197-204 (lab-1.5); lab-1.6 L157-172; student-lab/variables.tf L45-56
- **Evidence:** student-lab defaults: `node_count = 1`, `instance_type = "t3.xlarge"` (4 vCPU/16GB). On that one node, Lab 1.1 installs k0s + kcm + CAPI providers + cert-manager + Envoy Gateway + AWS CCM, then Lab 1.6 adds 'kof-operators (Grafana + OpenTelemetry operators)', 'kof-mothership (Grafana, VictoriaMetrics operator, Promxy)', 'kof-storage (VictoriaLogs cluster, Jaeger, PromxyServerGroup)', and 'kof-collectors (OpenTelemetry, kube-state-metrics, node-exporter)' plus opencost. The managed cluster is `controlPlaneNumber: 1` / `workersNumber: 1` with `instanceType: t3.medium` (4GB) and 50GB disks (lab-1.5 L197-204), and that single worker receives kof-child collectors (Lab 1.6) then cert-manager + kyverno + ingress-nginx via MCS (Lab 1.7). No document computes requests-vs-allocatable headroom, no troubleshooting entry covers Pending/OOMKilled from resource exhaustion, and no finder dimension (infra, pedagogy, floor, drilldown) examined sizing.
- **Fix:** Sum the actual resource requests of the cumulative Lab 1.5-1.7 stack against t3.medium allocatable (~3.4GB) and t3.xlarge allocatable (~14.5GB) on a real run; either bump the managed-cluster worker to t3.large/workersNumber 2 with a cost note, or document the expected memory pressure and add an 'Insufficient cpu/memory — Pending pods' entry to the troubleshooting tree. Without this, capacity failures are indistinguishable from the already-flagged wrong-expected-output defects.

### [major] Quality-automation/process dimension unexamined: zero CI (no .github), no link-check/shellcheck/terraform-validate gates, and stray committed duplicate directory trees prove nothing lints the repo — the root cause that lets the ~70 confirmed copy-paste defects recur
- **File:** `.github` Ln/a (directory absent)
- **Evidence:** `ls -la .github` → 'no .github' (no workflows, no CI of any kind in the repo). Meanwhile `git ls-files` shows accidentally committed nested duplicate trees: `lab-infrastructure/lab-infrastructure/docs/plans/CLAUDE.md`, `lab-infrastructure/lab-infrastructure/scripts/CLAUDE.md`, `lab-infrastructure/terraform/modules/networking/terraform/modules/iam/CLAUDE.md`, `lab-infrastructure/terraform/modules/networking/terraform/modules/shared-infra/CLAUDE.md` — evidence that no structural check ever runs. The defect classes the other finders confirmed (dead week-2 links, phantom `ui_url` output consumed by lab-status.sh, non-existent k0s subcommands, wrong resource names) are precisely the classes a markdown link checker, shellcheck, and `terraform validate` + output-reference cross-check would catch mechanically.
- **Fix:** Add a minimal CI workflow: markdown link check (lychee), shellcheck on lab-infrastructure/scripts/*.sh, `terraform fmt -check` + `terraform validate` per environment, and a grep gate that every `terraform output` name consumed by scripts/labs exists in outputs.tf. Remove the committed lab-infrastructure/lab-infrastructure/ and networking/terraform/ stray trees. Without this, the fixes from this audit will silently regress.

### [minor] Assessment delivery and instructor apparatus unexamined: the 'graded' Week 1 quiz ships its full answer key one click below the questions in the same student-facing file, with no submission/grading mechanism and no instructor or facilitator guide anywhere in the repo
- **File:** `curriculum/week-1-foundations/week-1-quiz.md` L4, 119-122
- **Evidence:** week-1-quiz.md L4: '**Passing Score:** 80% (15/18 correct)' — but L119-122 place the complete '## Answer Key' in the same file behind '<summary><strong>Click to reveal answers</strong></summary>'. There is no submission, proctoring, or grade-recording mechanism; Week 1 README's whole Assessment section is one line ('Complete the [Week 1 Quiz](week-1-quiz.md) after finishing all labs', L137). A repo-wide search finds no instructor/facilitator/trainer guide for Week 1 (matches exist only as incidental words in Week 2/5 lab prose). Prior audit's assessment lens reviewed question content (Q17/Q18/Q19), never the delivery model.
- **Fix:** Decide and state the delivery model: if self-paced, relabel the quiz as self-assessment and drop 'Passing Score' language (and reconcile with the pedagogy finding that calls Day 5 a 'graded assessment'); if cohort/graded, move the answer key to a separate instructor-only location and add a minimal facilitator guide (timing, common wrong answers, environment-reset procedure for students who failed mid-week).

### [minor] No pause/stop cost path exists or was examined: the only lifecycle verbs are provision/destroy, and the official cross-week guidance is 'simply disconnect' — leaving t3.xlarge + bastion + NAT + EIP + the managed-cluster VPC billing 24/7 with no documented EC2 stop-instances alternative
- **File:** `curriculum/week-1-foundations/labs/lab-1.8-upgrade-k0rdent.md` L636
- **Evidence:** lab-1.8 L636: 'If you need to pause between weeks, simply disconnect. Use `lab-connect.sh` to reconnect when you're ready to continue.' The scripts directory contains only lab-provision.sh, lab-destroy.sh, lab-status.sh, lab-connect.sh, lab-refresh-creds.sh — no stop/resume script, and a grep across Week 1 + lab-infrastructure for stop-instances/overnight guidance finds nothing for the k0rdent environment (lab-infrastructure/README.md L173 mentions overnight only for GPU environments). Existing findings flag the destroy-vs-never-destroy contradiction, but no finder examined the missing middle option that would resolve it: stopping the EC2 instances (which would cut the dominant compute cost while preserving state; NAT/EIP/EBS would still bill but at a fraction).
- **Fix:** Add a documented pause path: `aws ec2 stop-instances` for the management node and bastion (verify k0s and kubeconfig survive stop/start given the stable private IP and bastion EIP, and that CAPA-managed cluster nodes are handled or explicitly excluded), or an explicit statement of why stop/start is unsupported plus the real per-idle-day cost. This is the concrete fix the flagged cost-guidance contradictions point at but never land.


## Refuted during verification (recorded for transparency)

- **[drilldown]** Lab 1.5 splits AWS_REGION across two machines: the export+aws-CLI block only works on the local laptop (mgmt node IAM lacks ec2:DescribeKeyPairs), but the ClusterDeployment heredoc that expands ${AWS_REGION} must run in the SSH session — yielding an empty region
  - Refutation: Central mechanism refuted: the finding claims the mgmt node's IAM instance profile lacks ec2:DescribeKeyPairs (citing only iam/main.tf L120-138, the CCM block), forcing the export+aws block onto the local laptop and leaving ${AWS_REGION} empty in the SSH-session heredoc. But the same policy's CAPA block (Sid CAPIProviderAWSEC2, iam/main.tf L184-231) explicitly grants ec2:DescribeKeyPairs at L189; the mgmt node has the AWS CLI installed (mgmt-cloud-init.yaml L164-167) and the instance profile attached (k0rdent-mgmt/main.tf L223). So 'aws ec2 describe-key-pairs' succeeds in the SSH session, and the natural single-terminal reading of Lab 1.5 Parts 1-2 (kubectl commands in the same flow must run on the node) keeps export and heredoc in the same shell — region renders correctly. No forced two-machine split, no UnauthorizedOperation, no empty region under the lab's literal instructions. Residual minor truths: the L291 'from local machine with AWS CLI' comment is inconsistent framing, and the Resuming block (L74-89) doesn't re-export AWS_REGION (profile.d/k0rdent-lab.sh does not source lab-info.env), so resuming directly at Part 2 could yield an empty region — a minor resume-hygiene polish item, not critical, and adjacent to already-documented findings (C5 heredoc-expansion class; Lab 1.3 hardcoded us-east-1 breaking the Lab 1.5 key-pair lookup). Severity and root cause as stated are wrong.

- **[drilldown]** Lab 1.6 installs KOF chart version 1.5.0 on a k0rdent 1.2.2 management cluster; official k0rdent 1.2.x docs pin KOF 1.2.1 — unsupported 3-minor-version skew
  - Refutation: Refuted. The finding compares against the wrong documentation. The curriculum installs k0rdent ENTERPRISE 1.2.2 (oci://registry.mirantis.com/k0rdent-enterprise/charts/k0rdent-enterprise --version 1.2.2, per lab-infrastructure/terraform/modules/k0rdent-mgmt/templates/mgmt-cloud-init.yaml and Lab 1.1's 'Enterprise v1.2.2' table), not OSS k0rdent 1.2.x. Enterprise versioning is decoupled from OSS: the official k0rdent Enterprise 1.2.2 docs (docs.mirantis.com/k0rdent-enterprise/1.2.2/admin/kof/kof-install/) pin all KOF charts at exactly --version 1.5.0 (verified from raw page HTML; the lab's helm commands mirror those docs nearly verbatim). The cited OSS docs (docs.k0rdent.io/v1.2.0 -> KOF 1.2.1) apply to OSS kcm 1.2.x, a different version train. The finding's mechanism is also wrong: kof-mothership renders ServiceTemplates with apiVersion k0rdent.mirantis.com/v1beta1 in BOTH v1.2.1 and v1.5.0 (verified against chart source svctmpl.yaml at both tags), so there is no 'newer kcm API' rendering in 1.5.0, and the lab's L209-211 hedge is not evidence of skew. Not a duplicate: the prior audit explicitly validated the four-chart KOF 1.5.0 install as correct. Only residual nit (different, minor issue): the lab pulls 1.5.0 from the OSS ghcr.io registry instead of the Enterprise registry.mirantis.com registry used by the Enterprise docs.

