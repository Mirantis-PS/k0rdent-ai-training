# Week 5 labs 5.11–5.18 — validation plan and status

Goal: complete Week 5 live validation. Labs 5.1–5.10 were validated in April 2026;
5.11–5.18 had never been run against a live environment.

## Phase A — static source validation (DONE 2026-07-06, commit on this branch)

Eight parallel review agents (one per lab) executed runbook Part I §2 against upstream
sources: k0rdent Enterprise 1.3.x docs, ghcr/NGC OCI registries (`helm show chart/values`),
GitHub release tags, HuggingFace model API, NVIDIA/AWS/OTel/SchedMD/topograph docs.
Findings and fixes are in the commit message; the notable systemic results:

- `gpu-operator-25-10-0` stays the week-wide pin: the catalog's git listing dropped
  25.10.0 (now 24.9.2/25.3.0/25.10.1/26.3.3) but `ghcr.io/k0rdent/catalog/charts/gpu-operator:25.10.0`
  still resolves and the kgst install path (Lab 5.2) works. Re-check at the next bump.
- Labs 5.14 and 5.18 contained invented APIs (values schema / CRD / CLI) that could
  never have worked; both rewritten against pinned upstream releases (slurm 1.1.0,
  topograph 0.5.0).
- Every HuggingFace model referenced is ungated (NousResearch mirrors); no HF token
  blocker remains in 5.11–5.18.

## Phase B — live validation waves (PENDING; needs fresh AWS credentials)

Environment: resume `tapha`/eu-west-2 (`./scripts/lab-resume.sh tapha` — starts
mgmt `i-01e5f8f23754fccdb` + bastion `i-01431572ac166f43a` and refreshes in-cluster creds).
Enterprise 1.3.2, KOF 1.6.0. All child clusters via `aws-standalone-cp-1-0-20`.

### Wave 0 — no GPU (<$1, ~2 h)
- **5.17 (OTel export)** — t3.large child cluster labelled `kof-cluster-role: child`,
  throwaway in-cluster OTLP receiver (Jaeger all-in-one). Decisive check: does the MCS
  values patch (`opentelemetry-kube-stack.collectors.daemon.config`) MERGE into KOF's
  managed exporters or replace them ("extend, don't replace" premise).
- **5.18 (topograph, plumbing half)** — helm install 0.5.0 on any cluster; `/healthz`,
  `/v1/topology`, live svc name, `ec2:DescribeInstanceTopology` in the node role.
  A10G/no-NVLink means the accelerator label is absent by design; Part 6 perf stays doc-verified.

### Wave 1 — g5.xlarge (~$1/h)
- **5.12 (cluster templates)** — decisive test: deploy `ml-dev`, confirm whether the default
  `gpu-operator-25-10-0` ServiceTemplate flaps without k0s `toolkit.env` overrides
  (CONTAINERD_CONFIG=/etc/k0s/containerd.d/nvidia.toml etc. — see Lab 5.1/5.2); then either
  patch the four serviceSpec values blocks or restructure to manual install. Also capture:
  real clustertemplate list, CAPA node names (`<cluster>-md-<hash>`), `status.config` k0s version.
  Task 4 (2× p4d) is quota-gated → Wave 3 or doc-verified.
- **5.11 (FIPS)** — g5.xlarge. BLOCKED on a decision: positive FIPS outputs need Ubuntu Pro
  (token) or a RHEL/AL2023 AMI; on stock Ubuntu 22.04 only the negative branches fire.
  Verify template names resolve + gpu-operator deploys either way.

### Wave 2 — g5.12xlarge (~$5.7/h, ~4–6 h total)
- **5.14 (Slurm)** — slurmd are pods: one g5.12xlarge hosts all 4 single-GPU compute pods
  (p4d NOT needed despite the labs/README tier tag). Verify GPU GRES exposure
  (`scontrol show nodes | grep Gres`; may need Gres/AutoDetect in `extraConfMap`),
  MariaDB+accounting wiring, operator-generated names/labels, REST v0.0.43.
- **5.13 (TensorRT-LLM)** — single GPU; 7B INT4-AWQ fits A10G 24 GB → run on g5 instead of
  p4d (~$115 saved). Skip FP8 (needs CC≥8.9 — not even A100 qualifies). Check driver ≥575
  requirement of Triton 25.06 vs GPU Operator v25.10.0 driver. NGC login needed for nvcr.io pulls.
- **5.16 (distributed training, smoke)** — apply the two blocking fixes live (pip install
  deepspeed/transformers on workers; OMPI→torch env shim or torchrun c10d), reparametrize
  MPIJobs to np=4 on 1 worker, tiny model. Validates logic only; bandwidth/EFA/multi-node
  claims stay doc-verified unless Wave 3 runs.

### Wave 3 — p4d.24xlarge, us-west-2 (OPTIONAL — user budget call)
- **5.15 tier 1 (~$50)**: 1× p4d, 1.5 h — GPU Operator driver.rdma, EFA device plugin
  (`vpc.amazonaws.com/efa: 4`), single-node NVLink busbw ~200–230 GB/s. Quota: 96 P-vCPU us-west-2.
- **5.15 tier 2 (~$130)** + **5.16 full**: 2× p4d + placement group + AWS DLC image for real
  `NET/AWS-EFA` cross-node transport. Quota: 192 P-vCPU.
- Permanently doc-verified: Azure/InfiniBand halves of 5.15 (no Azure credential in env),
  cross-cloud MCS, gated Llama-2-70b task, NVSwitch/fabric-manager telemetry rows (5.17),
  NVLink perf uplift (5.18), p4d bandwidth tables (5.16).

### Inputs needed from the user — RESOLVED 2026-07-06
1. AWS credentials: SSO login done (profile docker-testing-437775732836, env-var creds only).
2. FIPS path for 5.11: **negative-branches-only** (lab gets a prerequisite callout that the
   positive outputs require Ubuntu Pro fips-updates or RHEL/AL2023).
3. Wave 3 budget: **tier 1+2 approved (~$180)** — quota already sufficient
   (692 P-vCPU in us-west-2, verified via service-quotas).

### Done criteria (per runbook §6)
Every lab's validation checklist executed live with outputs matching docs (or the lab
amended); teardown orphan checks empty; mgmt cluster re-paused; issues logged
symptom→cause→fix→re-validated; PR to main with live test plan.
